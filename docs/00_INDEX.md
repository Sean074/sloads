# sloads Documentation — Index

This directory is organised into six numbered sections by **document type**.
Lower numbers are the day-to-day references; higher numbers are planning and
historical record.

| Section | Type | Contents |
|---------|------|----------|
| `10_standard/` | **Code standard** | The authoritative description of how the suite works *today* — architecture, the per-module spec, the conventions charter, and the process guides. Update these whenever code changes. |
| `20_theory/` | **Theory & equation sources** | Where each module's equations and regression oracles come from (the `reference/` PDFs), plus per-module page citations as modules are ported. |
| `30_future/` | **Future development** | The mission-tagged backlog, the parked-items file, and the live plan documents for **open** work. Plans whose work has shipped move to `40_history/`. |
| `40_history/` | **Historic record** | What has shipped — completed modules/phases, key decisions, and resolved defects. |
| `50_reviews/` | **Process reviews** | Periodic development-process assessments and the recommendations driving process changes. |
| `60_guide/` | **Oracle GUI user guide** | Page-by-page illustrated guide to the oracle GUI (design note 34): front matter, one generated-and-authored chapter per oracle page in workflow order, worked-example appendices. Field tables and the chapter list are generated (`docs/generate_data_dict.py`); screenshots by `scripts/capture_guide_shots.py`. |

---

## 10_standard — Code standard

| File | Scope |
|------|-------|
| [`00_program_overview.md`](10_standard/00_program_overview.md) | **Start here** — program code standard & developer guide: structure, coding standards, error-handling contract, units, entry points, the **documentation-currency rule** (standard docs point at owners, never copy volatile values; this index ↔ the docs tree — both guarded by `tests/test_doc_currency.py`), testing/coverage |
| [`PROJECT_GUIDE.md`](10_standard/PROJECT_GUIDE.md) | Architecture, **the authoritative as-built package tree** (§4, guarded by `tests/test_package_layout.py`), porting conventions, validation strategy, dependency-ordered roadmap |
| [`PROGRAM_SPEC.md`](10_standard/PROGRAM_SPEC.md) | Per-module specification for all 22 programs plus the modern additions (inputs, outputs, FAR conditions, `.BAS` mapping); every registered module has a section, guarded by `tests/test_spec_coverage.py` |
| [`GUI_design.md`](10_standard/GUI_design.md) | **GUI design & structure** — navigation model, global sidebar, page anatomy/conventions, unit-boundary input pattern, definition-page standards, FAR 23 applicability/concept-awareness, JSON persistence |
| [`GUI_USER_GUIDE.md`](10_standard/GUI_USER_GUIDE.md) | **GUI user guide** — task-oriented walkthrough: workflow phases, what to enter where, the seed chain, LIMIT-vs-ULTIMATE reading rules, and an end-to-end `ga6_normal` example with hand-checkable numbers |
| [`SUMMARY_REPORT.md`](10_standard/SUMMARY_REPORT.md) | **Summary-report document standard** (Step G8) — purpose and audience, whole-document content rules (ultimate-load marking, traceability, axes/signs/stations, absence handling, units), the required section structure, the **excluded-content** list, and the conformance checklist |
| [`ORACLE_REPORT.md`](10_standard/ORACLE_REPORT.md) | **Oracle technical report content standard** (design note 44, OR-9) — the report generated from the oracle GUI: the issue package and its `SUMMARY_REPORT.md` §4.7 manifest, the derived section set and its four states (included / not yet implemented / excluded / absent) with their precedence, DRAFT-until-signed-off, provenance by anchors plus a versioned fingerprint, and the determinism rule with its timestamp qualifier. Cites `SUMMARY_REPORT.md` §2–§3 rather than restating them. Grows one row per agreed section (OR-8/G-OR-8) |
| [`DATA_DICTIONARY.md`](10_standard/DATA_DICTIONARY.md) | **`project.json` data dictionary** (generated) — every input field's type, units, default, owning page, and consuming modules; produced by [`generate_data_dict.py`](generate_data_dict.py) |
| [`CONVENTIONS.md`](10_standard/CONVENTIONS.md) | **Conventions charter** — axes/frames, unit channels, the LIMIT→ULTIMATE contract, case identity, preserved ENGLOADS signs, and the single-source-owner + drift-guard-test table; cite it in every physics/export design note |
| [`DEVELOPMENT_PROCESS.md`](10_standard/DEVELOPMENT_PROCESS.md) | **How work moves with more than one developer** (design note 28): trunk-based branches + protected `main` + squash PRs; closure travels in the PR (history entries as `changes/*.history.md` fragments); GitHub Issues as the record and `00_backlog.md` as the plan; design note as a PR before code; the three shared counters (schema, digest, case-ID bands) — rebase before you regenerate; CODEOWNERS; release manager; the AI with several developers |
| [`WORKFLOW_COMMANDS.txt`](10_standard/WORKFLOW_COMMANDS.txt) | **Workflow command crib sheet (plain text)** — copy-paste sequences for a release under the solo profile: opening the `dev/vX.Y.Z` milestone branch, closing each item on it with `solo_close.sh`, cutting the release and landing the milestone on `main` as one merge-commit PR, plus the recoveries. Summary of `DEVELOPMENT_PROCESS.md` §0, which wins on any disagreement |
| [`GIT_FLOW_GUIDE.docx`](10_standard/GIT_FLOW_GUIDE.docx) | **Git flow guide (Word)** — step-by-step commands with the reason for each, under the protected-`main` settings: the one-off rescue of stranded commits, the everyday issue → branch → PR → squash flow, rebasing a branch, recoveries, the owner's protection knobs today vs. with a second developer, cheat sheet. Companion to `DEVELOPMENT_PROCESS.md`; regenerate from the same source if the process changes |
| [`CODE_REVIEW_PROCESS.md`](10_standard/CODE_REVIEW_PROCESS.md) | Critical code-review process for module ports (tiered S/M/L + scoping, 2026-08-05) |
| [`RELEASE_PROCESS.md`](10_standard/RELEASE_PROCESS.md) | Versioning and release process (cadence rule + bounded gate, 2026-08-05) |

## 20_theory — Theory & equation sources

| File | Scope |
|------|-------|
| [`00_theory_sources.md`](20_theory/00_theory_sources.md) | The authoritative references (`reference/` PDFs) and how to cite them in code and tests |
| [`01_far25_gap_analysis.md`](20_theory/01_far25_gap_analysis.md) | **FAR 25 gap analysis** (Phase F25) — the FAR 23 → FAR 25 comparison table, per-condition disposition, and what stays out of scope (tuned-gust, continuous turbulence, full Appendix K) |
| [`02_approved_corrections.md`](20_theory/02_approved_corrections.md) | **Approved corrections register** — the authoritative list of deliberate oracle deviations (CLAUDE.md states the policy and links here) |
| [`balanced_cases.md`](20_theory/balanced_cases.md) | **Balanced free-free airplane cases** — the balancing method (applied set, pre-closure residual, six-DOF d'Alembert closure, handedness) with worked wing symmetric/antisymmetric examples, the design-of-record low-tail / T-tail empennage cases, the unsymmetrical horizontal tail, and the ground/landing families (§9 — the `n_z = 0` solve, the LANDLOAD identity, why `RESIDUAL_GATE` does not apply) |
| [`design_airspeeds.md`](20_theory/design_airspeeds.md) | **Design airspeeds (STRSPEED/MACHLIM)** — FAR 23.335/23.337 equations, the two 25.335(b) dive-speed routes, Mach-limit lines, and the Subpart-G operating-limitation (placard) relationship |
| [`engine_loads.md`](20_theory/engine_loads.md) | **Engine-mount loads (ENGLOADS)** — equations for FAR 23.361/363/371 with a worked IO-520-BB example |

## 30_future — Future development

| File | Scope |
|------|-------|
| [`00_backlog.md`](30_future/00_backlog.md) | **Authoritative backlog & development plan** — mission statement (concept loads → sbeam loop) + mission-tagged open items in priority order, the open-defects index, and the open design decision requiring user input (all 22 suite programs ported) |
| [`02_parked.md`](30_future/02_parked.md) | **Parked items** — real but off the mission path (M4 deferrals, F25-3/5, the long-tail L-items, future-direction placeholders); move an item back to the backlog before working it |
| [`01_concept_loads_plan.md`](30_future/01_concept_loads_plan.md) | **Phase C plan** — growing the suite into an initial-concept distributed-loads tool (concept mode, Schrenk airloads, per-component distributed loads, sbeam export bridge) |
| [`03_gui_rework_plan.md`](30_future/03_gui_rework_plan.md) | **Phase G plan** — workflow-aligned GUI rework: one-unit-per-dimension policy, single-source-of-truth geometry, re-sequenced analysis-flow navigation, and the new fuselage-moment/trim-plot/elevator-chord features (assessment vs. current code, locked decisions G-1…G-4) |
| [`09_distributed_empennage_loads_plan.md`](30_future/09_distributed_empennage_loads_plan.md) | **Distributed empennage loads design note** — h-tail/v-tail spanwise distributed loads on a user-defined LRA with `GRID`+`FORCE`+`MOMENT` export: decisions T-1…T-7 (reuse `SurfaceInput`, chord-proportional shape, uniform tail mass, smeared-then-discrete control-surface options, T-tail concurrent-load transfer to the fin tip), closed-form closure gates, and the T1–T8 step sequence |
| [`11_balanced_airframe_cases_plan.md`](30_future/11_balanced_airframe_cases_plan.md) | **Balanced full-airframe load cases design note** — free-free full-span balanced cases: measured ga6 baseline (0.32 % residual), decisions B-1…B-8 (mass SSOT, 2-DOF residual closure, **full-span primary deliverable**, left/right twins by reflection, `CaseRef` keying, lateral closure), the double-count authority table, steps B1–B9 |
| [`24_lra_beam_model_review_note.md`](30_future/24_lra_beam_model_review_note.md) | **LRA beam-model design review** (backlog band B, steps 12/13/14) — the user's eight-feature target model (hinges/actuators, wing SOB, fuselage beam + posts, fin and h-tail attachments incl. T-tail, LRA at 40 % / hinge line / section centre, gear and engine nodes) measured against the shipped export code and the written plans; the three-deliverable distinction (per-component decks / assembled equilibrium deck / LRA structural model), recommendations R-1…R-14, decisions **BM-1…BM-5**, and the per-file doc/schema update list. **AGREED 2026-08-15 — backlog and plan 09/10/11/21 amendments applied, no code** |
| [`21_power_effects_wing_note.md`](30_future/21_power_effects_wing_note.md) | **Power effects on the wing design note** (backlog Pri 14a) — thrust + propeller-wake loads for wing-mounted propellers: physics decomposition (DATCOM §4.6 with the Digital DATCOM ex3 printed oracle, `N_p`, slipstream strips, tail `q_h/q`/`Δε`), the case policy (`-P` variants, `SLIP-P`, 23.361/23.371 wing-box cases), GUI placement, re-trim-then-assemble generation, the measured proof that today's wing cases are exactly zero-thrust, the empennage routes, decisions P-0…P-12, gates, sequencing (prereq Pri 6 twins + CFR pull) and seams. **AGREED 2026-08-15 — PARKED except the hub thrust card, which SHIPPED 2026-08-17 (#10): one entered thrust per engine as an axial `FORCE` at the hub, closure-carried. Everything else here — `N_p`, slipstream, the DATCOM increments, the re-trim, the `-P` families — is unwritten** |
| [`32_oracle_gui_note.md`](30_future/32_oracle_gui_note.md) | **Oracle GUI design note** (decisions **OG-1…OG-13**) — a second, independently launched Streamlit front-end exposing only the original McMaster suite's inputs with CSV + text output, over the **same** `sloads/` calc: the measured baseline (295 distinct input paths, 22 of 23 modules already running on `ga6_normal`), the page set derived from `WorkflowStep.bas` and the `bas = "—"` sentinel defect it found, the shared-app-shell extraction that is the real no-dual-path prerequisite, the field-origin registry + drift guard, gates G1–G8 and steps OG-A…OG-F. **PROPOSED 2026-08-19 — no code; requires a recorded decision against the GUI freeze (#29)**
| [`34_oracle_user_guide_note.md`](30_future/34_oracle_user_guide_note.md) | **Oracle GUI user-guide design note** (decisions **UG-1…UG-8**) — the shape of a page-by-page illustrated guide to the oracle GUI, settled before fourteen chapters are written: a new `docs/60_guide/` section with one chapter per derived page in `sloads.workflow.oracle_steps()` order, per-page field tables generated from `field_registry.py` (prose never restates a units string or a default), re-runnable Playwright screenshots, and two worked examples carried through every chapter — `ga6_normal` plus a new FAR 23 light twin expressible entirely within the oracle GUI's own scope. `10_standard/GUI_USER_GUIDE.md` is **not** superseded: it stays the task-oriented guide to the full `app/` GUI (UG-2). Acceptance gates G-UG-1…G-UG-6, closure tier M. **AGREED 2026-08-26 (owner, in session), milestone 0.8.0 — nothing built** |
| [`44_oracle_report_note.md`](30_future/44_oracle_report_note.md) | **Oracle technical report design note** (decisions **OR-1…OR-37**, gates **G-OR-1…G-OR-19**) — a clean, modern formal LaTeX report of the oracle GUI's analysis, generated from a new `oracle_app` page (an explicit amendment to note 32's exclusion list): section set derived from `sloads.workflow.oracle_steps()`, content built in `sloads/report/oracle_content.py` over the existing renderer with nothing recomputed, computed pgfplots figures via the existing plot owners, `SUMMARY_REPORT.md` §2–§3 identity/content rules applied by citation. Development protocol is **one section at a time, owner-agreed per section** (OR-8), each agreement recorded in a new `10_standard/ORACLE_REPORT.md` with a guard test (OR-9). The agreed report becomes the starting point for the rebuilt main-GUI report. **§6 (added 2026-08-30, #151) carries three milestone-scoped decisions** on the same footing: **OR-13** freezes the solver (`sloads/modules/**`) and the five existing `oracle_app` files additive-only for the milestone's duration, enforced as a hash manifest by `tests/test_frozen_set.py` (**G-OR-9**); **OR-14** files a defect found in frozen code and leaves it in place; **OR-15** triages what is found to 0.8.2 / 0.8.3 / 0.9.0. All three lapse at the 0.8.2 cut. **§7 (added 2026-08-30) settles the report file and the report page** before iteration 1: **OR-16** the new `oracle_app/report.py` and the widened OR-13 admission that registers it; **OR-17** report metadata as its own `<stem>.report.json` artifact (`ReportSpec`, its own schema version) so one project yields many issues and `Project` is untouched; **OR-18** the title block and DRAFT-until-signed-off; **OR-19** section selection as stated exclusion, never omission; **OR-20** the document's unit system as a spec field; **OR-21** provenance by human identity + anchors + a versioned fingerprint over the oracle-consumed projection, warning and never refusing. Gates **G-OR-10…G-OR-13**. **§8 (added 2026-08-30) settles the issue package the build produces:** **OR-22** a written directory per issue (`report.tex`, the stamped `report.json`, a copy of `project.json`, `MANIFEST.txt`, `data/<step_key>.csv`) rather than a download; **OR-23** the shipped data is the document's source — `\input` table fragments and pgfplots reading the CSVs, so the document cannot disagree with it — each file self-describing (units with `-ULT`, SF and basis, step key, fingerprint); **OR-24** the package's spec is a stamped snapshot while the working spec stays beside the project; **OR-25** rebuild clobbers the revision, a new revision is a new directory; **OR-26** self-containment read at package level (the `SUMMARY_REPORT.md` §2 reading recorded as a decision) and the PDF compiled out of tree; **OR-27** the button is *Build*, DRAFT stays the unsigned state. Gates **G-OR-14…G-OR-17**. **§9 (added 2026-08-30) settles iteration 1 and supersedes OR-24:** **OR-28** the package directory is the spec's home (no `<stem>.report.json` beside the project); **OR-29** the report root is `<project dir>/reports/` with a discovered-package picker; **OR-30** the as-built stamp moves to a separate `build.json` so the build never rewrites the user's spec; **OR-31** iteration 1 is the front matter and the abstract is the author's, not the computed governing-loads summary; **OR-32** *not yet implemented* is a third gap state distinct from excluded and absent, with a precedence that changes once a section is built; **OR-33** `REPORT_SCHEMA_VERSION` holds at 1 for the milestone; **OR-34** the empty figure/table lists still render; **OR-35** `MANIFEST.txt` is a full `SUMMARY_REPORT.md` §4.7 manifest, widening G-OR-14; **OR-36** the PDF compile waits for a package-aware `compile_pdf`; **OR-37** no example report file. Gates **G-OR-18** (the gap states stay distinguishable) and **G-OR-19** (the build never rewrites `report.json`). **AGREED 2026-08-29 (owner, in session), milestone 0.8.2 — iteration 1 built 2026-08-30** |
| [`45_wtenv_aft_edge_note.md`](30_future/45_wtenv_aft_edge_note.md) | **WTENV aft-edge design note** (decisions **WE-1…WE-8**, gates **G-WE-1…G-WE-5**) — `WTENV.BAS` (Appendix C pp. 382–383, lines 180–500) sorts the discretionary items ascending, sweeps, re-sorts descending and sweeps again, printing a **forward and an aft edge** of the useful-load envelope with the item name and waterline on every vertex; the port implements the ascending sweep only and drops all three outputs. **WE-1/WE-2** complete it through one direction-taking sweep (no second copy of the walk), **WE-3** restores the per-vertex waterline (the item name is parked, amended 2026-08-31: `LoadValue` has no per-row string channel and `label` is cosmetic by M4-9), **WE-4** breaks station ties stably rather than reproducing the `.BAS`'s unstable sort — whose printed tie order is a function of the declared array size and cannot move a number — **WE-5** puts the oracle on a test-local transcription of the Appendix A p. 138 data base because `ga6_normal` is the *Chapter 3* database and its ballast lock (78/418/158) is computed from the no-baggage maximum, **WE-6** makes the additive claim a test rather than a promise, and **WE-7** leaves the ballast reference on the forward edge, so no delivered load moves. Both edges reproduce **Appendix A p. 139** on all three printed columns within ±0.1 %. Motivated by the oracle report's §2.2 figure (note 44 **WE-8**): on `ga6_normal` the forward edge never leaves the structural box while the aft edge reaches 87.32 in at 2743 lb, 2.2 in aft of the aft-gross CG limit — so a figure drawing the forward edge alone shows the half that approaches no limit. Frozen-file admission under note 44 **OR-15 row 1**; closure tier L. **AGREED 2026-08-31 (owner, in session); WE-3 amended the same day; SHIPPED 2026-08-31 in 0.8.2 (#157, `changes/wtenv-aft-edge.*`)** |
| [`46_applied_wing_load_set_note.md`](30_future/46_applied_wing_load_set_note.md) | **The applied wing load set** (decisions **OR-65…OR-70**, gates **G-OR-35…G-OR-38**) — Appendix B.1 and `wing_applied_loads.csv` stated three of the six components a body-axis load needs, and the sbeam deck built its `MOMENT` cards by **differencing the cumulative** `Myy`, which already contains the sweep and dihedral transfer of the shear carried outboard: under the rigid-body accumulation a solver performs, the exported wing torsion was wrong by **151/190/120 %** on `ga6_normal` (PHAA/TORS/ACRL) and **34/21 %** on `baron_58`, while shear and both bending columns closed exactly. **OR-65/OR-66** publish all six components with the three structural zeros printed and route the body-axis sign map through one owner (`applied_body_moments` over `bending_moment_vector`); **OR-67** rebuilds `wing_nodal_loads` from the applied set — each strip's own `fx`/`fz`/`myy_free` at its own node, each concentrated mass reduced to the node inboard of it as a force plus the **full** three-component `r × F` couple — which reproduces all five published columns at **every** station to ~1e-15; **OR-68** strengthens the deck's equilibrium claim from the bare card sum `m0.y` to the rigid-body `m.y`; **OR-69** makes each wing CSV state the moment convention it uses in-band; **OR-70** leaves B.2 unwidened (**superseded by note 47 OR-71**). `FORCE` cards are unchanged; no frozen file is edited. Closure tier L. **AGREED 2026-09-03 (owner, in session)** |
| [`47_appendix_b2_chord_bending_note.md`](30_future/47_appendix_b2_chord_bending_note.md) | **Appendix B.2 and chord bending** (decisions **OR-71…OR-75**, gates **G-OR-39…G-OR-43**) — note 46 filed **OR-70** ("B.2 is not widened") on the reasoning that a beam `Mzz` beside a body-axis `Mz` would mix conventions without a reader-visible reason. That reason does not survive: B.2 already prints `Mxx` beside B.1's `Mx`, and 3.2's notation table already carries the sentence that separates them. `Mzz` is oracle-locked (Appendix A p222), printed by `wing_span_loads.csv` and by 3.3, named by the closure gate the appendix is written under, and at the root **exceeds the torsion B.2 does print** on four of five example cases (`ga6_normal` ACRL 86,959 vs 48,244 lb-in). **OR-71** gives B.2 its fifth column and 3.2 its fifth recurrence, superseding OR-70; **OR-72** gives 3.4 its fifth figure, superseding OR-55's `Mzz` omission and tying the figure set to the column set by gate; **OR-73** has B.2's note restate the sign rather than only cite it. Found in passing: 3.3's heading "Root chord bending Mzz" named a symbol 3.2 did not define, and the notation guard walked only the appendix tables — **OR-74** makes the symbol data on the value (`LoadValue.symbol`, the third instance after `frame` and `point`) because the prose heading cannot be parsed, and **OR-75** widens the guard to all of section 3. Second **OR-15 admission** of 2026-09-03 (`net_loads.py`); no oracle moves, no digest moves. Closure tier L. **AGREED 2026-09-03 (owner, in session)** |
| [`48_limit_channel_note.md`](30_future/48_limit_channel_note.md) | **Module analysis is a LIMIT channel** (decisions **OR-76…OR-86**; gates **G-OR-44…G-OR-48**) — **#154** was filed as a small false claim (a `ConditionResult` holding no load still carries `safety_factor = 1.5`, so a geometry table prints an ULTIMATE banner); reviewing it found the factor applied on far more surfaces than the contract's purpose requires — the `engine` CLI report scales a mean takeoff torque 554.4 → 831.6 ft-lb. **OR-76** makes the CLI and the app's per-module views a LIMIT channel, ULTIMATE remaining the channel of case selection and the export deck; **OR-77** defaults the new channel parameter to ULTIMATE so the **frozen** `oracle_app/results.py` is unchanged by construction rather than by inspection; **OR-78** keeps the oracle technical report ULTIMATE throughout, which is what makes the change safe inside 0.8.2. **OR-86** adopts the owner's principle that the factor is **stated, never applied**, and splits its scope: 0.8.2 takes the module-view half, 0.8.3 removes the last multiply in its own boundary note — the field itself survives because two families (23.367(a)(2), 23.561(b)) are computed already-ultimate at SF = 1.0 and nothing else records that. **OR-81** makes LIMIT the global default with no per-artifact marker (retiring **M4-15** in 0.8.3, not before). **OR-83** puts the factorless test in one owner, `safety_factors.prescribes_factor` — *no load-unit value and no `case_ref`* — leaving `FAMILIES` untouched; measured, that is a stable **38** conditions on both GA6 and Baron 58, with `select`'s **6** critical wing cases protected by the `case_ref` clause. No `SCHEMA_VERSION` hop (`ConditionResult` is not persisted) and **no frozen file is edited**; the Imperial baseline is regenerated and must move in `txt/*` and `csv/*` only — 236 of 330 digests. The safety-factor GUI page is deferred to **0.8.3** (OR-85). Closure tier L. **AGREED 2026-09-04 (owner)** |
| [`49_stated_never_applied_note.md`](30_future/49_stated_never_applied_note.md) | **The factor is stated, never applied** (decisions **OR-87…OR-93**; gates **G-OR-49…G-OR-52**) — the 0.8.3 note note 48's **OR-86** commits to. Its central finding: **the sbeam deck is not a view but an input to a sizing program**, and no existing gate can tell the difference — the round-trip solve, the card-sum check and the closure invariant are all scale-invariant, so they stay green whatever the factor is, while a deck of limit loads sizes structure 1.5 low against ultimate allowables. **OR-87** therefore keeps the deck ULTIMATE and reaches OR-86's endpoint on a narrower line: *stated, never applied — on every surface a person reads*, still applied exactly once where a machine consumes it. **OR-89** moves the oracle technical report to LIMIT, closing a real defect (Appendix A is a limit oracle; the oracle tests compare at calc level and never cross the render boundary, so §3 prints 1.5× the manual and nothing notices). **OR-93** keeps the summary report ULTIMATE — it travels with the deck, so the export bundle is consistent by audience and nothing in it moves. **OR-88** inverts the `-ULT` marker onto the two already-ultimate families (23.367(a)(2), 23.561(b)); **OR-90** collapses M4-15 into the `methods_statement` stamp rather than retiring it, two channels still existing; **OR-91** keeps `LoadChannel`; **OR-92** leaves the safety-factor page (OR-85) to its own note. `CLAUDE.md`'s Phase C mission sentence and `SUMMARY_REPORT.md` §2 are unchanged. Closure tier L. **AGREED 2026-09-04 (owner)** |

## 40_history — Historic record

| File | Scope |
|------|-------|
| [`00_completed_development.md`](40_history/00_completed_development.md) | **Live** record of completed steps, key decisions, and resolved defects — the current release cycle plus the previous release-cut block only (tier M one paragraph, tier L full step format; tier S has no entry — its `changes/` fragment is the record). Older blocks roll into the frozen archives below at each release cut (`RELEASE_PROCESS.md` §4) |
| [`11_completed_development_to_0.5.0.md`](40_history/11_completed_development_to_0.5.0.md) | **Frozen archive** — everything that shipped before the 0.5.0 cut (2026-08-13), moved verbatim on 2026-08-16 (design note 26, backlog R11); do not edit |
| [`37_completed_development_to_0.7.1.md`](40_history/37_completed_development_to_0.7.1.md) | **Frozen archive** — the 0.7.0 cycle (everything that shipped between the 0.6.0 and 0.7.0 cuts) and the 0.7.0 release-cut block, moved verbatim on 2026-08-25 at the 0.7.2 cut (`RELEASE_PROCESS.md` §4 step 3 — the live file's second pass over the 1,500-line threshold); do not edit |
| [`41_completed_development_to_0.8.0.md`](40_history/41_completed_development_to_0.8.0.md) | **Frozen archive** — the 0.7.1 and 0.7.2 release-cut blocks (everything below the 0.8.0 cut block in the live record), moved verbatim on 2026-08-29 at the 0.8.1 cut (`RELEASE_PROCESS.md` §4 step 3 — the live file's third pass over the 1,500-line threshold); do not edit |
| [`43_application_point_note.md`](40_history/43_application_point_note.md) | **Ground-load application-point design note** (decisions **AP-1…AP-6**, gates **G-AP-1…G-AP-5**) — every ground case is transferred from the tyre contact patch, while LANDLOAD applies cases 1–12 at the **axle** and 13–24 at the **ground contact point** (its own printed column), so the twelve landing cases carry a spurious `r × F` pitching moment into the balanced deck, absorbed into the solved `q̈`. Adjudicated by an identity that never reads the point — `residual_My − G-7a lift moment == PITCHP` — which closes to ≤62 lb-in at the correct point and misses by 20,964–665,862 at the wrong one, on every bundled fixture, splitting exactly at the printed column's boundary. No reaction changes; the lever arm does. **AGREED 2026-08-29 (AP-1…AP-6, G-AP-1…G-AP-5); SHIPPED 2026-08-29 in 0.8.1 (#139, `changes/gear-application-point.*`)** |
| [`42_ground_frame_note.md`](40_history/42_ground_frame_note.md) | **Ground-frame design note** (decisions **GF-1…GF-8**) — the airplane-datum resolution of every attitude-1 ground case (braked roll, side, supplementary nose) rotates by **+GRA₂** where the level/tail-down families and the physics subtract it, a 2·GRA₂ ≈ 9.45° error on the body-frame reactions the exported deck cards and gear reference-point loads consume, adjudicated against the manual's own p230/p232 printout (an approved-deviation candidate, not an oracle break); plus the dual-frame reporting gap — `run()` ships only the ground-line set where the original prints both tables with a per-case fuselage-axis angle and datum load factors. Closure gates G-GF-1…G-GF-6 with the ga6 expected numbers; the practice-4 frame-rotation sweep (landing sites, flight-side α/β, export map). **AGREED 2026-08-29 (GF-1/GF-2′ owner-approved); SHIPPED 2026-08-29 in 0.8.1 (#133/#134/#139, schema v58, `changes/landload-beta-sign.*`, `changes/landing-body-frame-deliverable.*`)** |
| [`40_landing_load_factor_note.md`](40_history/40_landing_load_factor_note.md) | **Landing load factor design note** (decisions **LF-1…LF-12**) — the landing load factor is entered as **N**, not NLG: `gear_load_factor` is an NLG override that makes the wing lift factor `L` inert on `VMP` and leaves the page reporting an N the reactions were not computed from, so `N` and `L` become the inputs and `NLG = N − L` is derived, reported and never enterable. `L` uncapped for the FAR 25 basis (FAR defaults captioned, not enforced), `N ≤ L` refused, the 23.473(g) floors blocking in FAR 23 and warning in concept, `Optional` + "✕ clear" in both GUIs, and a semantic v56 → v57 hop `N = NLG_old + L` that moves no load number. Closure gates G-LF-1…G-LF-6 incl. p236/p230 oracle invariance and the `NVP == N` identity at 1e-9, closure tier L; §4 is the practice-4 sweep of the same defect class. **AGREED 2026-08-27; SHIPPED 2026-08-27 (#123, schema v57, `changes/landing-governing-n.*`)** |
| [`39_derive_override_note.md`](40_history/39_derive_override_note.md) | **Derive-by-default override design note** (decisions **OV-1…OV-12**) — one mechanism for the eight C210 duplicated-input findings: falsy-means-derive / typed-means-override at **calc level** through one named resolver per quantity (the `select.py:840` idiom generalized), the computed value shown beside each collapsed field via `derived_from` + `EXTERNAL_VALUES`, an OV-11 drift guard making the registry link mandatory, and additive schema fields (`tip_cap_width_in`, engine/prop mass-row selectors) with a 55→56 identity hop. Closure gates G-OV-1…G-OV-6 incl. oracle invariance and derive-equals-owner identities at 1e-9, closure tier L. **AGREED 2026-08-26; SHIPPED 2026-08-27 (#97, schema v56, `changes/derive-by-default-override.*`)** |
| [`38_taildist_aero_state_note.md`](40_history/38_taildist_aero_state_note.md) | **TAILDIST aero-state design note** (decisions **AS-1…AS-8**) — each TAILDIST condition states the aero state it distributes: `CriticalCondition` gains optional result fields `alpha_tail_deg` / `delta_deg` / `q_psf` beside the existing `beta_deg` (L-7.6 precedent: publish the state that made the load), populated per condition family with the values the method actually used — trim state for checked/gust, stated reasons where the method defines none — and AHT/AVT/EFFECTV printed once per component from single-owner slope functions (AS-5). Additive result fields, no schema hop (AS-7); no load number changes (AS-8). Closure gates G-AS-1…G-AS-5 incl. the Appendix A δ −5.39° oracle and the per-family closure identities, closure tier L. **AGREED 2026-08-26; SHIPPED 2026-08-27 (#100, C210-32, tier L, `changes/taildist-aero-state.*`) — the equivalent-gust-Δα extension parked at agreement (`02_parked.md`)** |
| [`35_completed_development_to_0.6.0.md`](40_history/35_completed_development_to_0.6.0.md) | **Frozen archive** — the 0.6.0 cycle (everything that shipped between the 0.5.0 and 0.6.0 cuts) and the 0.5.0 release-cut block, moved verbatim on 2026-08-23 at the 0.7.0 cut (`RELEASE_PROCESS.md` §4 step 3 — the live file's first pass over the 1,500-line threshold); do not edit |
| [`01_verification_baseline_0.2.0.md`](40_history/01_verification_baseline_0.2.0.md) | Verification baseline (0.2.0) — superseded by the 0.3.0 baseline below |
| [`02_verification_baseline_0.3.0.md`](40_history/02_verification_baseline_0.3.0.md) | **Verification baseline (0.3.0) — still the FAR 23 oracle authority** (carried forward unchanged by the 0.4.0 delta baseline below): the per-condition table of every checked FAR condition and the printed Appendix A (or worked-example) figure the suite locks against, plus the one-page oracle-vs-closure status table |
| [`03_resolved_decisions.md`](40_history/03_resolved_decisions.md) | **Resolved design-decision register** — D-1 … D-18 (all but D-5) with resolution date and a pointer to the rationale; open decisions stay in the backlog until answered |
| [`04_m4-1_body_moment_closure.md`](40_history/04_m4-1_body_moment_closure.md) | **M4-1 design note (closed 2026-08-03)** — fuselage body-load moment closure: diagnosis of the unreacted wing-attachment couple, the A–E options trade, the shipped carry-through distributed spar reaction (formulas, fallback, per-step verification figures) |
| [`05_phase_d_gui_workflow_plan.md`](40_history/05_phase_d_gui_workflow_plan.md) | **Phase D plan (executed; nav grouping superseded by Phase G)** — GUI workflow restructure: assessment, six-section target structure, load-case IDs, locked decisions D-1…D-7, page conventions. Still the citation target for the D-numbered GUI decisions |
| [`06_m3-1_rename_procedure.md`](40_history/06_m3-1_rename_procedure.md) | **M3-1 execution runbook (executed 2026-07-22)** — step-by-step git + shell procedure for the `farloads` → `sloads` rename batched with the `models.py` → `models/` split |
| [`07_m4_maintainability_sequence_plan.md`](40_history/07_m4_maintainability_sequence_plan.md) | **M4 maintainability sequence plan (executed 2026-08-03/04)** — execution order and per-step detail for M4-12 → M4-11 → G8 views → M4-10 → M4-9: measured baseline, design decisions D-12…D-18, per-step acceptance/risk, doc-sync matrix. Remainders M3-3b / M4-10b / M4-11b are carried in the backlog |
| [`08_verification_baseline_0.4.0.md`](40_history/08_verification_baseline_0.4.0.md) | **Verification baseline (0.4.0)** — a *delta* baseline: the closure gates for everything 0.4.0 added (mass SSOT, CONM2/MASSSET, balanced free-free cases, distributed empennage, the sbeam round-trip), the ten pinned exceptions with their measured figures, and what measurement changed. Carries the 0.3.0 oracle tables forward unchanged |
| [`10_backlog_state_narrative_to_2026-08-16.md`](40_history/10_backlog_state_narrative_to_2026-08-16.md) | The backlog's running "current state" narrative, archived verbatim on 2026-08-16 when the backlog header was cut to rules and pointers; unmaintained — `CHANGELOG.md` and the completed-development file are the records |
| [`09_verification_baseline_0.5.0.md`](40_history/09_verification_baseline_0.5.0.md) | **Verification baseline (0.5.0)** — a *delta* baseline: the closure gates for everything 0.5.0 added (six-DOF balanced closure, handed 23.427(a) twins, the lateral three, the report's own conformance gates, per-sheet workbook units), the nine pinned exceptions with their measured figures, the four standing limitations of a 0.5.0 deliverable, and what measurement changed. Carries the 0.3.0 oracle tables forward unchanged |
| [`12_verification_baseline_0.6.0.md`](40_history/12_verification_baseline_0.6.0.md) | **Verification baseline for release 0.6.0** — delta baseline: what 0.6.0 verified (the ground-family closure gate, the LRA model gates, the wing-tank tie as validator, the constants drift guards) with the gates at cut, carrying the 0.3.0 oracle tables forward unchanged |
| [`36_verification_baseline_0.7.0.md`](40_history/36_verification_baseline_0.7.0.md) | **Verification baseline for release 0.7.0 (current)** — delta baseline: what 0.7.0 verified (the oracle GUI's gates G1–G8 and the journey fixed point, the field registry's totality gate, the DATCOM-locked lateral body aero, the hub-thrust closed-form closure, the v55 reconciling migration, the widget-freshness and no-direct-`st.stop` guards) with the gates at cut, carrying the 0.3.0 oracle tables forward unchanged |
| | **Shipped design notes and plans** — rolled from `30_future/` at the 0.6.0 cut (`RELEASE_PROCESS.md` §4.3); each is the plan of record for its step, frozen as shipped: | |
| [`13_step_g8_summary_report_plan.md`](40_history/13_step_g8_summary_report_plan.md) | **Step G8 plan** — the consolidated loads summary report: locked decisions (LaTeX/PDF, pgfplots figures, methods-statement stamping, report depth), the `sloads/report/` package layout, seven ordered sub-steps, risks, and the test matrix. **✅ complete 2026-08-05** — the M3-3b remainder (`content.py`, `latex.py`, `plots_tex.py`, `export/pdf.py`) shipped; kept as the plan of record |
| [`14_m4-20_deliverable_units_plan.md`](40_history/14_m4-20_deliverable_units_plan.md) | **M4-20 plan — ✅ complete 2026-08-04** — deliverables render in the user-selected unit system: the human/solver **two-channel** unit split (N·m vs. the consistent N/mm/N·mm solver set), decisions D-19…D-22, seven sub-steps, the two `units.py` defects it fixes, and the test/doc-sync matrix |
| [`15_export_equilibrium_invariant_plan.md`](40_history/15_export_equilibrium_invariant_plan.md) | **Export equilibrium-invariant design note** — why R9's "Σ FORCE = n·W" cannot be asserted as written, and the per-deck resultant re-derivation that replaces it. **Shipped 2026-08-08**; carries a corrections block for the three places the plan was wrong |
| [`16_f25-2_speeds_placards_plan.md`](40_history/16_f25-2_speeds_placards_plan.md) | **F25-2 plan — ✅ complete 2026-08-08** — the 14 CFR 25.335(b) **Mach-margin dive-speed route** (`speeds.vd_basis`), the 0.07/0.05 margin policy and its single owner, the MC/MD single-source fix, decisions D-1…D-7, and the two defects it closed (concept `chosen_vd` silently overridden; CLI and GUI disagreeing about MNE/MFC) |
| [`17_sbeam_roundtrip_ci_harness_plan.md`](40_history/17_sbeam_roundtrip_ci_harness_plan.md) | **sbeam round-trip CI harness design note** — the standing solver gate: decisions S-1…S-9 (pinned-SHA `solver` extra, wing stick + test-only body/tail wrappers, determinate-support trick, negative mutation tests), the centerline-vs-side-of-body clamp analysis (§1.1), and the assertion tables |
| [`18_b8a_lateral_closure_plan.md`](40_history/18_b8a_lateral_closure_plan.md) | **B8a lateral-closure design note** (plan 11 step B8a, decision B-8) — the ±β empennage balanced cases: the lateral balance stated, the measured `n_y`/`ψ̈` baseline, the fin-waterline defect, the three `Izz` producers and their exact reconciliation identity, decisions **L-1…L-8** (open), and the replacement gate set G1–G8 |
| [`19_concentrated_wing_mass_nodal_split_plan.md`](40_history/19_concentrated_wing_mass_nodal_split_plan.md) | **Concentrated wing masses design note** — replacing nearest-node smearing in the exported wing bending with the exact per-mass nodal split (decision D-1, Option B offset couple), the `Mzz` sweep, and the equilibrium-closure gates. **✅ shipped 2026-08-09**; kept as the plan of record |
| [`20_sign_convention_report_section.md`](40_history/20_sign_convention_report_section.md) | **Sign-convention summary & report section design note** — the verified sign-convention extraction (state, controls, per-component loads, diagrams, engine/rotation), the DERIVED physical moment senses, decisions SC-1…SC-6 for the formerly absent conventions (β, rudder, twist, gear V/D/S, hand labels), and the report's required "Axes and sign conventions" section with three inline TikZ figures. **✅ shipped 2026-08-10**; kept as the plan of record |
| [`21_d_r8_unsymmetrical_htail_note.md`](40_history/21_d_r8_unsymmetrical_htail_note.md) | **23.427(a) handed balanced h-tail design note** (decision **D-R8**, review F-R5) — the third component branch of `build_balanced_cases`: decisions D-R8.1…D-R8.5 (SELECT's full split distributed and replacing the trim tail load, the residual as the maneuver, air-only applied set, handedness from the net applied roll, the closure's centroid reference) and the gate table. **✅ shipped 2026-08-10** |
| [`22_case_load_id_linkage_note.md`](40_history/22_case_load_id_linkage_note.md) | **Case identity ↔ deck LOAD id design note** (backlog Pri 2) — making the case id, the deck `LABEL` and the deck `LOAD`/`SUBCASE` integer joinable in the report and the GUI: the missing linkage, decisions 1–6 (**two** LOAD columns, one per deck family; `case_ids.deck_load_id` as the single owner; the no-`CaseRef` blank), the files that change and the invariant gates. **✅ shipped 2026-08-13** |
| [`23_step10_ground_cases_plan.md`](40_history/23_step10_ground_cases_plan.md) | **Step 10 ground/landing cases design note** — the decision record for the 0.6.0 headline: balanced free-free ground cases in the assembled deck, decisions G-1…G-14 (+G-7a lift along the ground line, +G-12a per-leg gear weight), the closed-form NVP/NDP/NS closure gate, the gear free-body report (contact patch + stroke), and the digest-wave predictions. **✅ COMPLETE 2026-08-15** — all three pieces shipped; kept as the plan of record |
| [`24_body_drag_carrier_note.md`](40_history/24_body_drag_carrier_note.md) | **Non-wing drag carrier design note** (backlog Pri 5) — the assembled model carries no non-wing drag, which is the whole pitch residual (through its `z` arm) and the whole `nx` gap: the wind-axis decomposition showing the gap is parasite drag (`ΔC_D ≈ −0.018` constant on ga6), why placing the load at the body mass centroid makes the residual **worse** (§3 — the trim lumps all drag at `zw`, and it is oracle-locked so it cannot be changed), the residual-versus-height tables and the **passing band** for both fixtures (§6.1), gates G1–G10 and decisions D-1…D-5. **Revision 2** rebuilds D-1's rationale (§8.1, the superseded one marked), drops its `root_waterline_z` branch as a latent gate-breaker on `ga6_normal`, and adds **G10** — the `ΔC_D` consistency gate that carries the signal placing the load removes from the residual. **✅ shipped 2026-08-15**; kept as the plan of record |
| [`25_d25_cgcase_loading_note.md`](40_history/25_d25_cgcase_loading_note.md) | **`CgCase` explicit loading definition** (decision **D-25** + D-25a…d, schema **v50**) — the loading behind a payload case becomes an input rather than a search over `2^n` discretionary subsets: the authoritative-loading/checked-echo contract, the `LoadingDefinition` form, the search kept as the fallback, and the 10 % ballast gate scoped to *solved* ballast only. **✅ shipped 2026-08-15**; kept as the plan of record |
| [`26_pri5_fixture_loadings_note.md`](40_history/26_pri5_fixture_loadings_note.md) | **Payload loadings for the four silent fixtures** (backlog Pri 5, decisions **D-26 / D-26a…c**) — why the row was not the data entry it looked like: the measurement showing five of six fixtures' CG cases miss what their own item database can produce by 15–60 in in station and 4–31 in in waterline, that three fixtures' heaviest case admits exactly one loading, and that closing it as written meant 10–44 % of each airplane as ballast at the spinner or the fin tip. Carries the three-defect diagnosis (unsourced `zcg`; unreachable all-up cases; a Cessna case lighter than the airplane), the four options with costs, the zoning that makes a **zero-ballast** answer reachable, and §7's record of what shipped — including the two export/assembly defects it uncovered and the fixture aero-data finding it filed. **✅ shipped 2026-08-15** — 2 → **6 of 6** fixtures assemble |
| [`27_lra_model_implementation_note.md`](40_history/27_lra_model_implementation_note.md) | **LRA beam model — implementation decisions of record** (step 12, LM-1…LM-7): the nearest-node + exact-lever-arm-couple transfer rule and its `coordinates.transfer_couple` owner, the concrete topology (SOB-start wing chains, split-fuselage posts, centre-box hub, attachment/gear/engine ties, the near-post support and why nose-clamping fails sbeam's SI conditioning), the id bands, the refusal list, the CI gates with expected behaviour, the import contract and the stated deferrals. **Shipped 2026-08-16 with the step** |
| [`28_doc_volume_reduction_note.md`](40_history/28_doc_volume_reduction_note.md) | **Documentation-volume decisions DV-1…DV-6** (agreed + shipped 2026-08-16, backlog R11): live history = current cycle + previous release block, older blocks in frozen archives; `CHANGELOG.md` `[Unreleased]` built from `changes/<slug>.<type>.md` fragments by `scripts/build_changelog.py` at release cut, never hand-edited; tier S = fragment + backlog removal, no history entry; the bounded history-roll step in `RELEASE_PROCESS.md` §4; guard test `tests/test_changelog_fragments.py` |
| [`29_static_typing_lint_note.md`](40_history/29_static_typing_lint_note.md) | **Static typing & lint decisions ST-1…ST-7** (agreed + shipped 2026-08-16): mypy over `sloads/` as a CI gate (own job), per-package strictness ratchet SSOT-owners-first, "narrow, never silence" rules of engagement, the widened ruff family set with every ignore reasoned; the measured before/after and the list of behaviour-affecting narrowing sites (all on already-refused paths) |
| [`30_multi_developer_process_note.md`](40_history/30_multi_developer_process_note.md) | **Multi-developer development, MD-1…MD-12** (agreed decision-by-decision + shipped 2026-08-16): trunk-based branches + protected `main` + squash PRs; "closure travels in the PR"; history entries as `changes/` fragments rolled at release; GitHub Issues + Project as the backlog record with `00_backlog.md` as the plan; design note as a PR before code; concurrency rules for `SCHEMA_VERSION`/digest/case-ID bands; CODEOWNERS; one `CLAUDE.md` for every developer's AI; the CONTRIBUTING / PR-template / issue-template / DEVELOPMENT_PROCESS build list; the four user calls recorded (1 reviewer + `self-merge-ok`; Issues + Project; `@Sean074` everywhere; built now) |
| [`31_wing_tank_fuel_separability_note.md`](40_history/31_wing_tank_fuel_separability_note.md) | **Wing-tank fuel separability design note, WF-1…WF-6** (agreed + shipped 2026-08-17, schema v53, backlog Pri 6 / #6): the same fuel pounds on both beams on three fixtures measured (3,800 / 4,000 / 1,200 lb, 7–15 % of the body beam); `MassItem.wing_fraction` (one additive field, schema hop, no migration) over a second row because the row is the WTENV loading-hierarchy unit; `reacted_parts` as the one owner of the row→parts split; the tie promoted to a validator; fixture fractions derived from WINGINER's own `concentrated` entries; predicted numbers, the acceptance gates, and §8's measured `Izz(closure)` +29–33 % on the three fixtures |
| | **Rolled from `30_future/` at the 0.7.0 cut** (2026-08-23): | |
| [`32_conm2_mass_export_plan.md`](40_history/32_conm2_mass_export_plan.md) | **CONM2 mass-export design note** — per-payload-case distributed mass as `CONM2`+`MASSSET` decks with a `GRAV`-based independent inertia-load check in sbeam: decisions C-1…C-6, the WTENV-derived itemizations, the mass unit-channel addition, and the no-double-count guard. **SHIPPED 2026-08-08** (C1–C5, C7; C6 with the round-trip harness); kept as the plan of record |
| [`33_l7_lateral_body_aero_note.md`](40_history/33_l7_lateral_body_aero_note.md) | **Lumped lateral body aero design note** (decision **L-7**, backlog Pri 13) — the fin is the suite's only lateral aero: DATCOM 5.2.3.1/5.2.1.1 `Cn_β`/`Cy_β` transcribed from the Digital DATCOM source (bundled chart data, closed-form `K_Rl`, ex1–ex11 as a printed **oracle**), the verified FAR 23.441(a) text (§4 — (a)(2) is a 1.3× overswing, source of SELECT's 19.5°, and it licenses a *static-stability* gate rather than a net-moment one), the measured `concept_regional_jet` derivatives and case effects, decisions L-7.1…L-7.17 and gates G1–G13. **Revision 2** carried three marked corrections to revision 1, including the shipped `n_y`-direction defect it uncovered (that defect **fixed 2026-08-15**); **revision 3** was agreed in chat on 2026-08-17 and is what shipped. **SHIPPED 2026-08-17 (#8)** — `sloads/lateral_body_aero.py`, `sloads/atmosphere.py`, the `select`/`balance` publication and the one off-by-default `body-aero` load, schema v54; rolled here at the 0.7.0 cut |
| [`34_derived_scalar_consolidation_note.md`](40_history/34_derived_scalar_consolidation_note.md) | **Derived-scalar consolidation design note** (decisions **DS-1…DS-7**) — one owner per quantity in the dataclasses: the measurement behind review finding CR-A-2 (ten multi-copy quantities in three classes, seven of them a non-persisted cache of `geometry`), the three defects the duplication caused (two modules resolving one wing area with opposite precedence, OG-7's unimplemented "entered scalar wins", a registry `derived_from` that documents a read-through the calc never had), gates DG-1…DG-5 and the measured cost. **SHIPPED 2026-08-21**; §8 executes DS-7 — the two class-C pairs retired by the v55 hop with a reconciling migration (#52, 2026-08-22) |

## 50_reviews — Process reviews

| File | Scope |
|------|-------|
| [`2026-09-04_project_review.md`](50_reviews/2026-09-04_project_review.md) | **Full-project review at `dev/v0.8.2`** (milestone 0.8.2, findings **R-1…R-27**, filed as #172–#191): a two-phase pass — implementation, maintainability and process controls; then analysis-method accuracy, safety-factor application and third-party-analyst usability, with a triage of the whole backlog and every `30_future/` note. Verdict **no wrong number on any delivered load surface**, and an independent re-derivation of global equilibrium from the shipped balanced deck's own cards closed to 5.5e-8 (Imperial) / 2.2e-6 (SI) across all 44 subcases on both fixtures. The first-order findings are deliverable-facing: the LRA decks for `ga6_normal` and `cessna_210` do not solve in the pinned sbeam and the roundtrip gate covers only the two fixtures that pass (R-1, #172), the shipped methods statement declares 3 of 7 approved oracle deviations behind a guard that checks itself (R-3, #174), and the balanced deck ships `SOL 101` + `SPC = 1` over zero elements with the explanation only in a test docstring (R-2, #173). Process verdict: drift is guarded superbly, closure weakly — three of the last four tier-L notes were never flipped to SHIPPED and 0.8.0 was tagged on a red main matrix (R-13/R-14). §4.4 carries the recommended ordering per milestone; Appendix A records where not to look |
| [`2026-08-27_oracle_gui_production_review.md`](50_reviews/2026-08-27_oracle_gui_production_review.md) | **Oracle GUI production-release review** (milestone 0.8.0, open — owner decisions pending): a four-pass pre-cut review of the oracle GUI's production surface (`oracle_app/` + `app_shell/` + `oracle.py`, 3,807 lines) and the release gates in front of it. Verdict **not ready to tag** on five BLOCKS-CUT rows, four tier S and none physics — band B not empty (#124), the §3.5 smoke gate never launching the oracle GUI, design notes 32 and 35 describing shipped work as unbuilt on the eve of rolling to history, the %MAC↔station Tool reinterpreting its station on a unit switch (the one converted input outside the unit boundary), and the `Development Status` classifier. Three defect-class sweeps (#71 mid-entry, blank-Optional #121/#122, blank rows in every list) returned 0 failures in 154 page renders — recorded as where not to look |
| [`2026-08-24_oracle_gui_code_review.md`](50_reviews/2026-08-24_oracle_gui_code_review.md) | **Oracle GUI code review + backlog re-cut** (milestone 0.7.2, open — owner decisions pending): a code-level pass over `oracle_app/` + `app_shell/` (the C210 review's blind side) plus a `30_future` re-cut into 0.7.2 / 0.8.0 (oracle-GUI development) / 0.9.0 (main-GUI development and bug correction) / 1.0.0. Two live first-order defects, both in `render_table`'s row-count widget — rows deleted with no confirmation or undo, and a blank row attached that breaks the flight envelope and saves to disk; the negative results (units channels, the unit boundary, widget identity) recorded as where not to look |
| [`2026-08-23_c210_oracle_gui_build_review.md`](50_reviews/2026-08-23_c210_oracle_gui_build_review.md) | **Cessna 210 from blank — oracle GUI build review** (milestone 0.7.1, in progress): the owner types a 210L/M from public data into the shipped oracle GUI, Geometry → Wing Loads → Landing Loads, then Export from `app/`; every finding classed *a* (interface does not work → 0.8.0 repurposed), *b* (bug → 0.7.2) or *c* (development → later); data sheet, estimates register and findings log |
| [`2026-08-22_pre_cut_beta_review.md`](50_reviews/2026-08-22_pre_cut_beta_review.md) | **Pre-cut beta review — the oracle GUI's function end-to-end** (issue #61, findings PB-1…PB-24): the fresh-project journey on all 14 oracle pages (two examples typed from blank, results and downloads compared byte-for-byte, save/reload/re-run bit-identity), the `oracle_app/` + `app_shell/` delta since `4b1ddcc`, and gates G1–G8 re-checked for rot — eight BLOCKS-CUT findings (`Project.mass` never produced, untagged weight items on the fuselage beam, G5's reduction not the oracle GUI's project, blank-seeded selector names and a free-text category, an unreloadable engine-layout save, the stale project download, no project name so every save overwrites the last) and sixteen known-issues for the release notes |
| [`2026-08-22_backlog_review_0_7_0_beta.md`](50_reviews/2026-08-22_backlog_review_0_7_0_beta.md) | **Backlog review before the 0.7.0 cut — the beta scope re-cut** (findings BB-1…BB-10): 0.7.0 re-scoped as a beta release of the oracle GUI; band A repopulated with #51 (#50 closed as duplicate) + #44 as one pass, #45 promoted on a measurement (wrong first-run guidance on 2 of 14 oracle pages), and #52's v55 duplicate-retirement hop; the `app/views/` freeze carve-out and the schema-freeze amendment; nothing promoted from the parked file |
| [`2026-08-20_critical_review.md`](50_reviews/2026-08-20_critical_review.md) | **Critical four-pass review at `4b1ddcc`** (findings `CR-A/B/C/D-<n>`): Oracle GUI, core analysis, above-oracle layer, main GUI & CI as four independent parallel passes plus a cross-pass synthesis — no CRITICALs, ten MAJORs in three seams (GUI data-entry integrity, guard blind spots over core invariants, statements-about-the-deliverable), the 08-10 closure audit (one rot instance), the gates-read-the-shipped-artifact structural lesson, and the ranked critical/high-leverage cut of the already-filed backlog |
| [`2026-08-17_constants_and_conversions_review.md`](50_reviews/2026-08-17_constants_and_conversions_review.md) | Ownership review of every hard-coded physical constant and unit factor (deg/rad, g, in/ft, in²/ft², V²/295, atmosphere, hp, π, gust 498) — one owner per quantity, exact-by-default value policy, and the `constants.py` (Imperial↔Imperial) vs `units.py` (Imperial↔SI) demarcation; feeds backlog Pri 8 |
| [`2026-08-17_backlog_review_0_7_0.md`](50_reviews/2026-08-17_backlog_review_0_7_0.md) | **Backlog review after the 0.6.0 cut — the 0.7.0 re-cut** (issue #30, findings BR-1…BR-13): band B rows measured against rule 6, L-7 lateral body aero as the 0.7.0 headline with the fixture-data pass first, the gust-shape study merged into the decisions row, the GUI review (#29) added, note 12's status corrected, the schema rule for 0.7.0 |
| [`2026-08-16_gui_user_review.md`](50_reviews/2026-08-16_gui_user_review.md) | **User GUI review — in progress** (Sidebar, Start and Input swept; Flight/Other/Ground/Plotting/Export still to write): a page-by-page user assessment of the GUI, each row carrying a `GR-<SECTION>-<n>` key and a class — **[P]** placement, **[I]** input model, **[D]** decision of record. Promoted after the 0.6.0 cut: the GUI freeze and the schema freeze both hold until then, and the [I] rows land as one 0.7.0 input-model step |
| [`2026-08-16_scope_and_deficiency_review.md`](50_reviews/2026-08-16_scope_and_deficiency_review.md) | Scope and deficiency review against the "fidelity above the base analysis" concern: every open backlog row and the shipped capability sorted into needed-now / future-release / park, the step-14 descope and band-E/H parking recommendations, the effect-vs-error-bar ordering rule, and the code-health findings CH-1…CH-8 |
| [`2026-08-15_review_0_6_0_candidate.md`](50_reviews/2026-08-15_review_0_6_0_candidate.md) | Critical review of the 0.6.0 candidate (everything since the v0.5.0 tag): findings R6-C\*/R6-T\*/R6-D\* — deliverable-contract gaps in the new gear report channel, the missing G-6 rotational gate half, and the documentation-currency defects, with the recommended pre-0.6.0 order of work |
| [`2026-08-10_code_review_0_5_0.md`](50_reviews/2026-08-10_code_review_0_5_0.md) | Pre-0.5.0 full code review: findings C1/F-\*/D-R\*/m1–m21 and the three-phase release plan they drove |
| [`2026-08-05_development_process_review.md`](50_reviews/2026-08-05_development_process_review.md) | Full-depth development-process audit (Jun–Aug 2026): findings F1–F7, recommendations R1–R11 behind the 2026-08-05 process changes, and the sbeam cross-project comparison |
| [`CODE_REVIEW_2026-07-21.md`](50_reviews/CODE_REVIEW_2026-07-21.md) | Point-in-time code review (moved here from the repository root at 0.5.0) |
| [`PROJECT_REVIEW_2026-07-19.md`](50_reviews/PROJECT_REVIEW_2026-07-19.md) | Point-in-time project review (moved here from the repository root at 0.5.0) |

## 60_guide — Oracle GUI user guide

Structure per design note 34 ([`30_future/34_oracle_user_guide_note.md`](30_future/34_oracle_user_guide_note.md), decisions UG-1…UG-12): chapters follow `sloads.workflow.oracle_steps()` order; per-page field tables and the chapter list live under `60_guide/_generated/` (written by `docs/generate_data_dict.py`, never hand-edited); screenshots in `60_guide/img/` come from `scripts/capture_guide_shots.py`. Gates G-UG-1…G-UG-6: `tests/test_guide.py`.

| File | Scope |
|------|-------|
| [`00_index.md`](60_guide/00_index.md) | Guide front page — what the oracle GUI is and is not, how to read a chapter, the two worked examples, the generated chapter list |
| [`01_getting_started.md`](60_guide/01_getting_started.md) | Install, launching the oracle GUI, the sidebar in full (units, open/save/download, the dirty indicator) |
| [`02_before_you_start.md`](60_guide/02_before_you_start.md) | The data to collect before opening the tool, as a checklist keyed to the chapters that consume each item |
| [`03_conventions.md`](60_guide/03_conventions.md) | Axes/signs (pointing at `CONVENTIONS.md`), stations and datum, the unit boundary, the one LIMIT-vs-ULTIMATE statement (UG-8) |
| [`01_configuration_layout.md`](60_guide/01_configuration_layout.md) | Chapter 1 — Geometry (`WINGGEOM`) |
| [`02_weight_mass.md`](60_guide/02_weight_mass.md) | Chapter 2 — Weight & Mass Properties (`WTESTIMA`+`WTONECG`+`WTENV`) |
| [`03_aero_coefficients.md`](60_guide/03_aero_coefficients.md) | Chapter 3 — Aerodynamic Data (modern page; sourcing ladder per UG-11) |
| [`04_structural_speeds.md`](60_guide/04_structural_speeds.md) | Chapter 4 — Structural Speeds (`STRSPEED`+`MACHLIM`) |
| [`05_flight_envelope.md`](60_guide/05_flight_envelope.md) | Chapter 5 — Flight Envelope V-n (`FLTLOADS`+`SELECT`) |
| [`06_wing_loads.md`](60_guide/06_wing_loads.md) | Chapter 6 — Wing Loads (`AIRLOADS`+`WINGINER`+`NETLOADS`) |
| [`07_fuselage_loads.md`](60_guide/07_fuselage_loads.md) | Chapter 7 — Fuselage Loads (`NETLOADS`) |
| [`08_tail_loads.md`](60_guide/08_tail_loads.md) | Chapter 8 — Tail Loads (`TAILDIST`+`BALLOADS`) |
| [`09_aileron_loads.md`](60_guide/09_aileron_loads.md) | Chapter 9 — Aileron Loads (`AILERON`) |
| [`10_flap_loads.md`](60_guide/10_flap_loads.md) | Chapter 10 — Flap Loads (`FLAPLOAD`) |
| [`11_tab_loads.md`](60_guide/11_tab_loads.md) | Chapter 11 — Tab Loads (`TABLOADS`) |
| [`12_engine_mount.md`](60_guide/12_engine_mount.md) | Chapter 12 — Engine Mount Loads (`ENGLOADS`) |
| [`13_one_engine_out.md`](60_guide/13_one_engine_out.md) | Chapter 13 — One Engine Out (`ONENGOUT`) |
| [`14_landing_loads.md`](60_guide/14_landing_loads.md) | Chapter 14 — Landing Loads (`LGFACTOR`+`LANDLOAD`) |
| [`A_worked_single.md`](60_guide/A_worked_single.md) | Appendix A — `ga6_normal` end to end (Imperial), against its Appendix A printed figures |
| [`B_worked_twin.md`](60_guide/B_worked_twin.md) | Appendix B — the light twin end to end (SI), closing on the unit channel (UG-12) |
| [`C_troubleshooting.md`](60_guide/C_troubleshooting.md) | Appendix C — run-order messages, "cannot run yet" notes, unit-toggle surprises, the dirty flag |
| [`D_where_next.md`](60_guide/D_where_next.md) | Appendix D — what the full `app/` GUI adds and when to move over |

---

> Root-level docs live outside `docs/`: [`../README.md`](../README.md) (user
> front page), [`../CONTRIBUTING.md`](../CONTRIBUTING.md) (the human on-ramp),
> [`../CHANGELOG.md`](../CHANGELOG.md) (release notes — `[Unreleased]`
> is generated from [`../changes/`](../changes/README.md) fragments at release cut), and
> [`../CLAUDE.md`](../CLAUDE.md) (guidance for Claude Code). The authoritative
> theory/oracle PDFs live in [`../reference/`](../reference/), alongside the
> **CFR/AC text extracts** cited by the calc and the F25 gap analysis —
> `14CFR_factor_of_safety.md`, `14CFR_operating_limitations.md`,
> `14CFR_MC_MD_speed_margin.md`, `14CFR_Part25_engine_torque.md`,
> `AC_23-19A_engine_torque.md`, `23_427_unsymmetrical_candidate_set.md`, and
> `fuselage_pitching_moment.md`.
