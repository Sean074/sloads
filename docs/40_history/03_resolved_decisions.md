# Resolved Design Decisions (register)

The permanent record of project design decisions that were **put to the user and
resolved**. Open decisions awaiting user input stay in
[`../30_future/00_backlog.md`](../30_future/00_backlog.md) (§ *Open design
decisions requiring user input*); once answered, they move here.

Full rationale for each decision lives in the step record in
[`00_completed_development.md`](00_completed_development.md) and in the plan
document named in the *Rationale* column.

| ID | Decision | Resolved | Rationale |
|----|----------|----------|-----------|
| D-1 | Concept reference airplane = swept twin-turbofan regional jet (forces the AIRLOAD4 swept branch) | 2026-07-16 | [`01_concept_loads_plan.md`](../30_future/01_concept_loads_plan.md) |
| D-2 | Concept gyroscopic rates: keep 23.371(b) fixed rates + guard/warn on exceedance | 2026-07-16 | Phase 1 Step P1-5 (history) |
| D-3 | No sbeam-VLM validation backend; closure + fleet plausibility. **Revisit trigger:** an OpenVSP/VSPAERO aero import would supply the cross-check whose absence this decision accepted (see the backlog's *Future directions*) | 2026-07-16 | [`01_concept_loads_plan.md`](../30_future/01_concept_loads_plan.md) |
| D-4 | Fleet comparison set (29 aircraft) sufficient as-is | 2026-07-16 | Phase F Step F1 (history) |
| D-6 | ~~Keep "FAR23LOADS"~~ **Superseded → full rename to `sloads` at 0.3.0 (M3-1)** | 2026-07-20 | [`06_m3-1_rename_procedure.md`](06_m3-1_rename_procedure.md) |
| D-7 | sbeam export: load-cards-only default; assembled stick model behind a flag (scopes long-tail **L-1**) | 2026-07-16 | [`01_concept_loads_plan.md`](../30_future/01_concept_loads_plan.md) |
| D-8 | Full-airplane project JSONs are the canonical input form; per-module slices derived | 2026-07-16 | [`../10_standard/PROJECT_GUIDE.md`](../10_standard/PROJECT_GUIDE.md) |
| D-9 | 23.427 unsymmetrical search: restore the full `SELECT.BAS` candidate set incl. unchecked (shipped as M1-4) | 2026-07-20 | [`../20_theory/02_approved_corrections.md`](../20_theory/02_approved_corrections.md) |
| D-10 | Aero-coefficient curve plot: include (**shipped as M4-5**, 2026-08-05) — supersedes the 2026-07-15 decline. Scope decided at implementation: as-entered curves at the reference Mach; balanced points overlaid by *recovery* from their dimensional output; the "recovered-CL closure" is a **drift guard**, with the stall-clamp margin as the content-carrying gate; entry checks are advisory `validation` warnings; **no** moment-slope check | 2026-07-20 | [`00_completed_development.md`](00_completed_development.md) (M4-5); `sloads/aero_curves.py` |
| D-11 | Backlog restructured to release milestones; rename ships with the release as **sloads 0.3.0** | 2026-07-20 | M3-2 (history) |
| D-12 | `LoadValue.key` is a **persisted** field (`io.py` `CriticalCondition.loads`), so **M4-10 lands before M4-9**: the key arrives via a normal label→key backfill migration, making M4-9 the migration chain's first customer | 2026-08-03 | [`07_m4_maintainability_sequence_plan.md`](07_m4_maintainability_sequence_plan.md) |
| D-13 | `htail_balance` returns a `typing.NamedTuple` with **lowercase** attributes (`lt25`, `lt50`, `at`, `delta`, `lt`, `cp`); Ref 1 Ch 9 symbols kept in the field docstrings | 2026-08-03 | [`07_m4_maintainability_sequence_plan.md`](07_m4_maintainability_sequence_plan.md) |
| D-14 | Cross-module private symbols are promoted by **underscore-drop in place + `__all__`** per module — no `sloads/api.py` facade; `_envelope` gets a chosen name rather than a mechanical strip. **Applied 2026-08-03 (M4-12b):** the chosen-name carve-out took three symbols, not two — `_envelope` → `default_envelope`, `_design_inputs` → `design_inputs`, and `_sigma` → **`density_ratio`** (a public `sigma` would have collided in meaning with `constants.standard_atmosphere`'s sigma, which it duplicates — logged as M4-23) | 2026-08-03 | [`07_m4_maintainability_sequence_plan.md`](07_m4_maintainability_sequence_plan.md) |
| D-15 | The `Project.tail_loads`/`.vtail_loads` property proxies are **documented (not replicated) in M4-12b and retired in M4-10**, where `project_from_dict` is rebuilt anyway | 2026-08-03 | [`07_m4_maintainability_sequence_plan.md`](07_m4_maintainability_sequence_plan.md) |
| D-16 | `unit_number_input` reads the active unit system through a **single `_active_system()` resolver** over session state; M4-20 re-pointed that one function at the `Project` field (step 2), and step 6 removed the twelve views that had been reading the session key directly | 2026-08-03 | [`07_m4_maintainability_sequence_plan.md`](07_m4_maintainability_sequence_plan.md) |
| D-17 | **`radon` added to the `dev` extra** to measure M4-11 before/after complexity — a reporting tool, explicitly **not** a CI gate | 2026-08-03 | [`07_m4_maintainability_sequence_plan.md`](07_m4_maintainability_sequence_plan.md) |
| D-18 | Consolidated test helpers expose **three named functions** — `value_of`, `load_value`, `values_by_label` — each accepting a `ModuleResult`, `ConditionResult`, or list of either | 2026-08-03 | [`07_m4_maintainability_sequence_plan.md`](07_m4_maintainability_sequence_plan.md) |
| D-19 | The sbeam solver deck uses **one dimensionally consistent unit set** — N / mm / **N·mm**, extended in step 4 to **MPa** (N/mm²) for pressure. Human-readable deliverables keep N·m / kPa, so a bundle carries two moment units *deliberately*, one per channel. A deck whose GRIDs are millimetres and whose FORCE cards are newtons is only correct with N·mm moments; an N·m one (or a kPa stress) is wrong by 1000× in a file that parses cleanly and sizes structure | 2026-08-04 | [`06_m4-20_deliverable_units_plan.md`](../40_history/14_m4-20_deliverable_units_plan.md) §2 |
| D-20 | SI design pressure is **`kPa-ULT`** in the human channel (the docs' `Pa-ULT` was amended); the solver channel's is `MPa-ULT` per D-19 | 2026-08-04 | [`06_m4-20_deliverable_units_plan.md`](../40_history/14_m4-20_deliverable_units_plan.md) §2 |
| D-21 | The **in-band unit statement wins** over byte-identical Imperial output: every file gains a units line, and the Imperial-unchanged guard is therefore strip-and-compare rather than raw equality | 2026-08-04 | [`06_m4-20_deliverable_units_plan.md`](../40_history/14_m4-20_deliverable_units_plan.md) §2 |
| D-22 | The sidebar Imperial/SI toggle **writes `Project.unit_system`** and marks the project dirty — the selection is a project edit, not a session preference, so it saves and reloads with the project | 2026-08-04 | [`06_m4-20_deliverable_units_plan.md`](../40_history/14_m4-20_deliverable_units_plan.md) §2 |
| D-23 | **A wing case row states the flight condition its loads were computed at.** Where `WingLoadCase` states its own `v_eas_kt`, that speed — the one `net_loads._air_cl_v` builds the air load from — is what `wing_case_ref` puts on the `CaseRef`, even where SELECT named the same condition at a different V-n point (`atr42_100`: `PHAA` entered at 170 kt, SELECT's point 185.85 kt). SELECT's **`case_id` is kept** (M4-2 decision 1: one ID per physical condition, which the case-index dedupe assumes), as are CG, altitude and the FAR reference, which the case does not state. Rejected alternative: keep naming SELECT's V-n point, on the grounds that the id names that condition — a deliverable must not name a speed its numbers were not computed at | 2026-08-13 | Backlog priority 1 (the envelope single-owner sweep, review F-C6); [`00_completed_development.md`](00_completed_development.md) |
| D-24 | **Pressurization is out of scope for sloads — permanently, not deferred.** No cabin differential-pressure case (14 CFR 23.365 / 25.365) is computed and no pressure load is combined with a flight or ground case; the standing limitation is restated as an *exclusion* rather than a gap, and stays in the pinned key set for the life of the tool. **M4-6 (step 10) is re-scoped** to the ground/landing distributed loads and the gear-reaction cards alone, which is what the mission's sizing loop actually needs. Two consequences are recorded rather than assumed: (a) the "for pressurized airplanes ground cases **cannot** be down-selected against flight" rule leaves with it, so how ground cases interact with the governing-set down-selection is now an **open question for step 10's design note**, to be decided on its own merits; (b) **F25-5**'s pressurization half (Part 25 ΔP combination rules) is closed as not-planned — its 23.415/25.415 ground-gust half stays parked. Unaffected: `WeightInput.pressurized`, a structural-weight allowance in the WTESTIMA regression and not a load case | 2026-08-14 | Backlog priority 1 (step 10) scoping session; [`00_completed_development.md`](00_completed_development.md) |
| D-25 | **`CgCase` gains an explicit loading definition** — which discretionary items are aboard plus the ballast (and its waterline) that closes the case. Rejected alternative: correct the six fixtures' CG cases to loadings their weight databases can already produce. On a reference airplane the CG-envelope **corner point is the real engineering input**; rewriting it to suit the item database would trade a true statement about the airplane for a convenient one, and the loading is the derived quantity. Unblocks the two sibling findings (only 7 of 18 payload cases derivable; only 2 of 6 fixtures produce a balanced case) and takes the assembled balanced deck from **2 fixtures to 6** in CI. Schema bump + migration; the two coverage pins are the closure gate and are re-pinned by design | 2026-08-15 | Backlog Pri 9 ([`../30_future/00_backlog.md`](../30_future/00_backlog.md)); 2026-08-15 priority re-cut; **shipped 2026-08-15** (schema v50) — see D-25a…d below |
| D-25a | **The entered loading is authoritative; the case's `weight_lb`/`xcg`/`zcg` become a checked echo of it.** What is exported is the loading's own properties — never a set bent to hit the nominal corner point — and a disagreement beyond `max(0.5 lb, 0.1 %)` / `0.5 in` (the search's own `_CG_MATCH_TOL`, whose precedent is ga6's CG4 at station 73.0924 against an entered 73.09) is reported loudly by `mass_distribution.case_loading_checks` and on the Weight & CG page. No ballast is ever *solved* for an entered loading | 2026-08-15 | [`../40_history/25_d25_cgcase_loading_note.md`](../40_history/25_d25_cgcase_loading_note.md) §2 |
| D-25b | **Form:** item references into `weight.items` + per-item fractions for `consumable` rows + an optional explicit ballast row (`LoadingDefinition`). `EMPTY`/`MINIMUM` rows are implicitly aboard — a loading is a statement about the *useful load* — and the weight database stays the mass SSOT (plan 11 B1), so nothing here duplicates a weight | 2026-08-15 | note §3 |
| D-25c | **Optional, with the subset search as the fallback.** `loading is None` keeps today's derivation bit-for-bit, so every pre-v50 file loads and behaves as it did; the absent key is both the old shape and the live "derive it" state, so a re-save of an older file keeps its bytes | 2026-08-15 | note §2 |
| D-25d | **The 10 % `BALLAST_CREDIBLE_FRACTION` gate applies to *solved* ballast only.** An entered ballast row is an engineering statement (real stress/flight-test ballast), so it exports whatever its fraction — with that fraction stated in the case note, the report's payload-case table and the deck `$` header, rather than hidden | 2026-08-15 | note §2 |
| D-26 | **Where a fixture's CG case is not a loading its own weight database can produce, the case data is corrected to the database** — amending D-25's "the fixtures' corner points stand". Measured 2026-08-15 before entering anything: on five of six fixtures the entered cases miss the database by 15–60 in in station and 4–31 in in waterline, and on `cessna_210`, `dhc8_dash8` and `concept_heavy` the heaviest case weighs *exactly* the all-up weight, so exactly one loading produces it and its CG is not a choice. D-25's mechanism cannot bridge that: closing Pri 5 as written would have meant entering 10–44 % of each airplane as ballast at the propeller spinner or the fin tip. Three sub-decisions below. `ga6_normal` is **not touched** — it already sits on its own database to 0.10 in and 0.42 in, and remains the standing proof that the search route (D-25c) is unchanged | 2026-08-15 | Backlog Pri 5; [`../40_history/26_pri5_fixture_loadings_note.md`](../40_history/26_pri5_fixture_loadings_note.md) |
| D-26a | **`zcg` is a derived echo of the loading, not an independent corner point.** WTENV's weight–CG envelope is weight against *station*; the waterline had no envelope behind it and on five fixtures was a round figure entered independently of the item waterlines — 6–31 in above the loading's real waterline on the flight cases and 20–27 in *below* it on the `atr42`/`dhc8` ground cases, inconsistent in sign. Freeing it alone took those two fixtures from 11–26 % ballast to 1–12 % | 2026-08-15 | note §4(a) |
| D-26b | **The lumped payload rows are zoned, preserving each fixture's discretionary totals.** `Passengers (n)` and `Baggage / cargo` split into fwd/aft cabin and fwd/aft hold at real stations, chosen so the group's `Σw`, `Σwx` and `Σwz` are unchanged — so every full-database total, and the all-up CG, moves by nothing. Without it a database of two or three point masses admits **one** loading at gross weight, so the fwd and aft corner cases collapse onto each other; with it there is genuine CG travel and **every case is a pure loading with no ballast at all**. The point-mass inertias about the airplane CG do rise, which is the physically correct consequence of a cabin no longer being a point, and `mass.cases` is re-derived from the items | 2026-08-15 | note §5 option B |
| D-26c | **`cessna_210`'s usable fuel is corrected to the type's long-range figure** (416 lb → 720 lb total, 120 US gal), and its `CG4` case, entered at 2,300 lb against an empty-plus-pilot-plus-reserve weight of 2,474 lb, is re-entered at 2,474. The fuel row is why: its database summed to *exactly* MTOW, so no two loadings of that airplane weighed the same and neither the flight nor the landing fwd/aft pair could exist. Every real airplane's tanks plus seats plus hold exceed MTOW — it is why load sheets exist — and `database_total`'s own docstring already says so | 2026-08-15 | note §4(b)/(c) |
| G8-5 | Summary-report `revision` is **free text** the engineer maintains, not a tool-managed auto-incrementing counter — a tool counter would disagree with the drawing/report system of record the moment a project is copied, and revision identity belongs to the engineering process | 2026-08-04 | [`05_step_g8_summary_report_plan.md`](../40_history/13_step_g8_summary_report_plan.md) §10 |

| F25-2-a | The 25.335(b) **Mach-margin dive-speed route is concept category "C" only**. FAR 23.335(b)(4) would permit it for N/U/A as well; it is withheld so the Appendix-A-oracle-locked FAR 23 path is *provably* untouched, and `vd_basis = "mach_margin"` in a FAR 23 category raises rather than silently falling back. Extending it to N/U/A (and the dormant commuter tier) is a backlog item | 2026-08-08 | [`08_f25-2_speeds_placards_plan.md`](../40_history/16_f25-2_speeds_placards_plan.md) |
| F25-2-b | The route is selected by an **explicit `vd_basis` enum**, not inferred from whether the chosen VD happens to clear a margin. The governing rule stays visible in the project file, and no existing project changes behaviour on load | 2026-08-08 | [`08_f25-2_speeds_placards_plan.md`](../40_history/16_f25-2_speeds_placards_plan.md) |
| F25-2-c | **MC/MD are single-sourced in the same step** rather than filed: `MachLimitInput.mc/md` are removed and `structural_speeds.design_speed_values` is the sole producer. The margin is a statement *about* MC and MD, so two copies (which the CLI and the GUI already disagreed about) could not be left standing beside a rule that compares them | 2026-08-08 | [`08_f25-2_speeds_placards_plan.md`](../40_history/16_f25-2_speeds_placards_plan.md) |
| F25-2-d | Mach-margin policy: **0.07 M default, 0.05 M absolute floor, written rational-analysis basis required in between** (flagged in results, in `validation.py` and with a GUI certification-risk warning). The floor constrains what may be **declared**; a chosen VD short of the declared margin is **raised** to meet it, like every other design-speed minimum in STRSPEED | 2026-08-08 | [`08_f25-2_speeds_placards_plan.md`](../40_history/16_f25-2_speeds_placards_plan.md); [`../../reference/14CFR_MC_MD_speed_margin.md`](../../reference/14CFR_MC_MD_speed_margin.md) §5 |
| F25-2-e | **VB is input-only.** 25.335(a) is `VC ≥ VB + 1.32·U_REF`, and U_REF (25.341(a)(5)(i)) does not exist in the suite until F25-1 — so only the ordering is checked, and the deferral is stated in the module docstring, the reference extract and the gap-analysis row rather than shipping half a margin check silently | 2026-08-08 | [`08_f25-2_speeds_placards_plan.md`](../40_history/16_f25-2_speeds_placards_plan.md) §3 |

| D-R1 | **0.5.0 is held for the deliverable** — no interim tag. The release cuts when the 2026-08-10 review's release scope (backlog rows 1–19: correctness & gates, deliverable completion, report basis/limits, mechanics) closes. Knowingly past the cadence rule's ~5-step threshold; the accepted risk is the missing regression baseline, and the mitigation is keeping the window short (~4–7 sessions) | 2026-08-10 | [`2026-08-10_code_review_0_5_0.md`](../50_reviews/2026-08-10_code_review_0_5_0.md) §4 |
| D-R2 | The **balanced free-free deck and the CONM2 mass model are first-class 0.5.0 deliverables**: CLI targets, G8.3 methods stamps, Export-bundle + manifest rows, and a balanced-cases report section (per-case n, residuals, hand pairs, mass-case identity). Ends the state where the mission's primary deliverable was page-only, unstamped and invisible to the controlling document | 2026-08-10 | review §4 (D-R2), findings F-D1/F-D2 |
| D-R3 | **Ground/landing case families are out of 0.5.0.** The body deck ships flight-only with the exclusion stated in-band (limitations block, deck `$` headers, release notes). Steps 10–11 (gear reactions, pressurization, balanced ground cases) are the 0.6.0 headline. Rationale: the 0.5.0 hold (D-R1) is for making shipped things right — S/M coherence fixes — not for new design-note-gated physics | 2026-08-10 | review §4 (D-R3) |
| D-R4 | The chordwise tail deck **maps the fin's load to Fy** via `coordinates.tail_force_to_airplane`, matching the span-deck family — the two tail families no longer disagree about the fin's axis, and the `coordinates.py`-named splice trap closes with numbers, not a comment. Byte-changing; `test_tail_deck_resultants` re-pinned to Fy; rides the shared digest regeneration | 2026-08-10 | review §4 (D-R4), finding F-C3 |
| D-R5 | The **CLI wing export transfers to the LRA** (hand the writers the `Project`, so `loads_ref_axis_results` runs) — headless and GUI wing decks become identical, per the existing module contract; the deck's `torsion_axis` is pinned by test. The 25 %-chord CLI behaviour is retired, not optioned | 2026-08-10 | review §4 (D-R5), finding F-C2 |
| D-R6 | `concept_heavy`'s export failure is **diagnosed before it is fixed**: run the envelope single-owner sweep (review F-C6) first, re-test `--export-sbeam`, then fix whichever side is actually broken — fixture data (missing `case` references) or the derived-case route. Either way the fixture then joins the round-trip matrix and the Imperial baseline (closing the digest-coverage gap in the same motion) | 2026-08-10 | review §4 (D-R6), finding F-D4 |
| D-R7 | Balanced-deck SUBCASE ids are **minted from the `CaseRef`** (`subcase_id` + handedness suffix), retiring the positional `BALANCED_SID_BASE + i` — the M4-2 stable-identity rule now covers every deck family, before consumers adopt the flagship deck. Byte-changing; drift guard added; rides the shared digest regeneration | 2026-08-10 | review §4 (D-R7), finding m1 |
| D-R8 | The **23.427(a) unsymmetrical h-tail balanced family is built in 0.5.0**, not merely filed — a third component branch in `build_balanced_cases` with left/right twins via the assembly reflection machinery. User decision against the file-and-state recommendation, accepting the release's one L-tier physics step (~1–1.5 sessions); practice 1 applies in full (design note agreed in chat before code) | 2026-08-10 | review §4 (D-R8), finding F-R5; **shipped 2026-08-10** — design note [`../40_history/21_d_r8_unsymmetrical_htail_note.md`](../40_history/21_d_r8_unsymmetrical_htail_note.md) |

| BM-1 | **SOB source:** an explicit `SurfaceInput.sob_y_in` (butt line, per surface — the h-tail attachment reads the same quantity), falling back to `geometry.parametric.fuselage_width/2` **marked assumed** — the fallback has data on the three real types since T-8a — and **never** `wing_mass.inboard_rib_y`, which is the WINGINER mass-panel start, not a body dimension | 2026-08-15 | [`24_lra_beam_model_review_note.md`](24_lra_beam_model_review_note.md) §5 / R-3; recorded at step 12 phase 0 (2026-08-16) |
| BM-2 | **Fuselage load-path idealization: split fuselage** — forward body a cantilever ending at the front-spar post, aft body + empennage a cantilever ending at the rear-spar post, statically determinate; the continuous beam on two rigid posts (indeterminate) is a step-14 opt-in, and the deck header states which one the file is | 2026-08-15 | note 24 §5 / R-4, R-12 |
| BM-3 | **Missing attachment data: the LRA exporter refuses**, it does not silently default — refuse an unset `ref_axis_pct` and, for the h-tail, gate on `TailSpanResult.attachment_basis` (refuse `ATTACH_STRIP_PAIR`, accept-and-state `ATTACH_OUTLINE`, accept `ATTACH_FIN_TIP` outright — **not** on `attachment_assumed`, which the outline branch always sets); assumed geometry it does accept (section centre, spar fractions) is stated in the header | 2026-08-15 | note 24 §5 / R-6, R-7c |
| BM-4 | **Gear / engine parent:** an explicit `mounted_on: fuselage \| wing` on the leg and engine inputs; inference by butt line vs `sob_y` is the marked-assumed fallback, never the primary | 2026-08-15 | note 24 §5 / R-8, R-9 |
| BM-5 | **Node/element identity contract:** every special node carries a `$ SLOADS-NODE <family> <side>` tag on export and the same tags (or a sidecar map) are accepted on import, nearest-node-per-family as the marked-assumed fallback; one production band per node family (`lra-sob` first, this step) and a production `RBE2` EID band distinct from the round-trip harness's test-only 900001+ range | 2026-08-15 | note 24 §5 / R-10 |
| WF-1 | **Wing-tank fuel separability is one additive field, `MassItem.wing_fraction`, not a second item row.** The row is the WTONECG/WTENV loading-hierarchy unit (`kind`, `consumable`, D-25 `fractions`, the `2^n` discretionary subset search); a second discretionary row would double that search and let it fly wing-full/body-empty to hit a CG. Only the reaction partition needs to split. Schema v53, additive with a `0.0` default, no migration hop — the 0.6.0 freeze's one hop | 2026-08-17 | [`../40_history/31_wing_tank_fuel_separability_note.md`](../40_history/31_wing_tank_fuel_separability_note.md) §2 |
| WF-2 | **Semantics:** the fraction of a row's weight *and* own inertias reacted by the wing (both sides together), the remainder by `component`; both parts at the row's position, so WTONECG/WTENV read rows and are unchanged to the last digit; a fraction (not pounds) so burn-down leaves the split invariant; non-zero on a `WING` row and values outside `[0, 1]` are validation errors | 2026-08-17 | [`../40_history/31_wing_tank_fuel_separability_note.md`](../40_history/31_wing_tank_fuel_separability_note.md) §2 |
| WF-3 | **One owner, `mass_distribution.reacted_parts`,** is the only place a row becomes the masses the beams react; `distribution()`, `balance` and the CONM2 header read it; `component_of` is exact only on a part; a drift guard pins consumer agreement. CONM2 cards stay one per row (a card is one mass at one position) | 2026-08-17 | [`../40_history/31_wing_tank_fuel_separability_note.md`](../40_history/31_wing_tank_fuel_separability_note.md) §2, §8 |
| WF-4 | **The wing tie is a validator** (`wing_mass_tie_open`, both signs, pounds + remedy), not only a test and a page caption | 2026-08-17 | [`../40_history/31_wing_tank_fuel_separability_note.md`](../40_history/31_wing_tank_fuel_separability_note.md) §2 |
| WF-5 | **Fixture fractions are derived from WINGINER's own `concentrated` entries** (3800/9174, 4000/4660, 1200/5500) — no number invented; the twins' fuel-system truth (all-wing fuel on an ATR) is out of scope | 2026-08-17 | [`../40_history/31_wing_tank_fuel_separability_note.md`](../40_history/31_wing_tank_fuel_separability_note.md) §2, §6 |
| WF-6 | **The WINGINER per-component wing deck does not move** — the fix is on the item side; wing-deck bytes are an acceptance gate | 2026-08-17 | [`../40_history/31_wing_tank_fuel_separability_note.md`](../40_history/31_wing_tank_fuel_separability_note.md) §2 |
| D-27 | **The load analysis evaluates the weight/CG limits the user defines: a FLIGHT case is a WTENV structural-limit point by construction**, seeded by `cg_cases.seed_flight_cases` — the four FLTLOADS.BAS prompts (aft gross, fwd gross, fwd regardless, minimum weight) plus one mid-envelope gross-weight case — with the closing loading *derived* (subset search + solved ballast under D-25d's 10 % gate). Supersedes D-26's "a case is the database's own extreme": the envelope owns *where* the case sits, the database owns *whether* it is reachable. Root cause it closes: the four type fixtures' fuselage item stations were hand-entered (2026-07-15) on a datum ~20–65 in aft of the wing polyline's, so every D-26 case sat 15–25 in aft of the entered aft limit at 51–65 % MAC; deriving the limits would have codified a 60 %-MAC aft limit, accepting them would have excluded every case flown. Rejected alternative: moving the wing (`le_root_x`) — the wing/tail arms are the oracle-relevant geometry, the item stations were only ever "plausible" | 2026-08-17 | chat (fixture-data pass, #9); `changes/fixture-cg-datum-reconciliation.*` |
| D-27a | **Item stations are less authoritative than the wing polyline and are the datum that moves.** Fuselage-carried rows shift uniformly onto the wing datum, sized so the empty CG sits at a type-plausible %MAC (`cessna_210` 18, `atr42_100`/`dhc8_dash8` 25, `concept_regional_jet` 35 — the user's stated targets; for a real analysis the user owns the item weights and the OEW CG); wing-tied (wing, fuel, wing-mounted engines/nacelles/propellers/gear) and tail rows do not move; the C210 nose group and the RJ's engines/gear/crew are placed by hand and held | 2026-08-17 | as above |
| D-27b | **Cases pinned to the limits, cases inside the envelope, items inside the outline — all three are guards, not prose.** `test_the_fixture_flight_cases_are_the_wtenv_seed` / `..._ground_cases_are_the_landing_seed` (the fixtures *are* the seeds), `test_every_fixture_case_sits_inside_its_wtenv_envelope`, and `mass_item_outside_body` (`validation._check_mass_items_in_body`, fore/aft extent of the fuselage outline for `fuselage`-carried rows; wheels hang below the belly and the linear three-section outline has no height at a pointed nose, so `y`/`z` stay the three-view's to show). Enforcement stays warning + CI guard, not a refusal | 2026-08-17 | as above |
| D-27c | **Case set per fixture: WTENV 4 + landing 3 + `mid gross`.** `ga6_normal` keeps Appendix A's own `CG1`..`CG4` (the seed reproduces them to the printed rounding, which is the check that the seed is Ch 3 p21's definition); `concept_heavy` has no envelope and keeps its one entered loading | 2026-08-17 | as above |
| D-28 | **No combined flight + ground station envelope — permanently, not deferred; the two families are delivered separately and combining them is the consumer's act.** Closes the follow-on **G-9** filed (issue #11) as decided-against rather than built. Reason, on the fuselage: flight and ground cases are assessed with **different internal-pressure companion cases**, so a station extreme from one family and a station extreme from the other belong to different total load states — a pointwise `max()` over both would present a number no single design condition produces. sloads excludes pressurization permanently (**D-24**), so it cannot form the correct combined state from its own outputs at all: this is unsupportable, not merely less useful. The **wing and empennage** carry no such companion, but the deliverable stays per family uniformly — one rule, not a per-component exception. Reinstates on its own merits the "for pressurized airplanes, ground cases cannot be down-selected against flight" rule that D-24 recorded as departing with the pressurization scope. **Not** argued on safety factors: G-9's original factor argument was retracted by **G-10** (both families are limit × 1.5) and is not part of this decision. What the user gets instead: each family's own critical set, as today — SELECT's governing conditions for flight, LANDLOAD's six per-FAR critical-reaction summaries plus the 33-case matrix and the assembled ground decks for ground — with the separation stated in the `ground-flight-separate-families` limitation. The ground family's missing per-station view is filed as **#31** (band B), where it stays a *per-family* deliverable | 2026-08-18 | chat (#11 closure); `changes/no-combined-station-envelope.*` |
| D-29 | **Derived `ACRL` names SELECT's own 23.349(a)(2) pick, and the derived route is a first pass — not a sizing case.** With `wing_mass.cases` empty, `wing_inertia.resolve_wing_cases` derives the wing cases from `envelope.critical`; its `ACRL` point carries CL ≈ 1.30 at 117.4 kt where the worked example (Ref 1 p217-221) enters CL 1.55 at 116 kt — a ~19 % air-load difference for the same named condition — and `unbal_moment = 0`, because SELECT's condition does not name the unbalanced rolling moment (it comes from AILERON, Ch 13). Decided: **SELECT's pick is what the derived `ACRL` names**, and the divergence is accepted and stated rather than reconciled. Rejected alternative: bending the derived route onto the worked example's point — there is no printed oracle for the derived route, the two entries answer different questions, and no shipped example is affected because **every fixture enters its cases explicitly and explicit always wins**. Consequence, stated where the route is offered: an `ACRL` case used for sizing is **entered**, never derived; the derived route is a first pass. `tests/test_wing_case_derivation.py::test_the_acrl_divergence_is_the_documented_one` stays the pin, now pinning a decision rather than an open defect | 2026-08-18 | chat (#13 closure); `changes/recorded-decisions.*` |
| D-30 | **The ATR-42's Mach-capped points are ordinary stall/Mach-limited flight, not a defect; what is wrong is that their coefficients are evaluated past the fit and nothing says so.** Nine of 300 balanced points (`MAN A` / `MAN C` / `AC ROLL` at 25,000 ft on aft/fwd/mid gross) sit up to +0.27 above the Mach-adjusted stall CL because the local Mach is pinned at MC and the dynamic-pressure iteration has no lever left. Decided, on the user's ground: **an airplane commonly cannot reach its manoeuvre load factor at altitude and is stall-limited through the speed range — that is normal**, and the regulation says so itself. **23.333(b)**'s manoeuvring envelope applies "*except where limited by maximum (static) lift coefficients*" (Ref 1 p62, Ch 8 *The Regulations*; Ref 2 §11.2.1.2 p68), so a point above the Mach-adjusted stall CL lies **outside** the envelope the rule defines — it is not a design condition the airplane fails to reach. Design speeds are EAS — "*the selected design air speeds are equivalent air speeds (EAS)*" (**23.335**, Ref 2 §7.2.1 p45) — **except** a.(4), "*at altitudes where an MD is established, a cruising speed Mc limited by compressibility may be selected*" (p46), which is exactly this fixture's MC = 0.4555: the Mach cap that produces the corner is the regulation's own provision, not an artefact. So n = 2.5 stays a design condition at the altitudes where it is attainable and nothing is owed at 25,000 ft. The point is nonetheless **not** re-reported at a reduced "attainable" load factor — not because the rule demands the higher number (it does not), but because the shipped matrix's other rows are at the requested n, the higher CL is the conservative side, and FLTLOADS' own clamp works by raising speed rather than lowering n; changing that is a physics decision with its own design note, not a rider on this one. Retires the earlier framing that "the loads reported at those points are not physically attainable" — `nz = n` is enforced by the alpha iteration, so the load factor and the total `n·W` are exact and the entire effect is on the LZW/LT split (<= 0.5 % of `n·W`). The real finding, measured 2026-08-18: the balance closes at alpha 14.1–16.3 deg against a fitted stall edge of 13.18 deg, so CM and CD are **extrapolated 0.9–3.1 deg past the range their fits cover**, and re-evaluating with the coefficients frozen at the fit edge moves the published tail quantities by 3.3–44 % (LT), 6.5–20.4 % (LT25/LT50), 8.5–20.8 % (elevator load) and 1.1–4.8 deg (elevator deflection) — above the 5–10 % base-method band, and a **lower** bound, since freezing is the mildest alternative and not the truth past stall. Ranked, not blocking: **0 of the 9 are SELECTed as a governing critical condition**, so no sizing case, critical-condition table or exported deck load moves anywhere — but BALLOADS publishes all 300 points, so 9 published rows carry the extrapolation unmarked. Filed as **#32** (band B, tier M): the rows stay **published and marked**, the marker derived at publication from the point's own CL against its Mach-adjusted stall CL, so no schema field and no stored state. The solver's own silence — both iteration loops return their last iterate with no signal — is the internal half, filed as **#33** (band C). The fixture is **not** edited to hide the corner: it is real data about a real airplane | 2026-08-18 | chat (#13 closure); `changes/recorded-decisions.*` |
| D-31 | **The gust cases reuse the manoeuvre spanwise shape (Schrenk); the gust-specific shape is recorded as decided, not filed as work.** The gust-vs-manoeuvre spanwise difference is **inside the Schrenk band by construction** — Schrenk *is* the base method's own approximation of the spanwise shape, good to ±5–10 % on a distributed load (`../20_theory/00_theory_sources.md` §Base-method uncertainty) — so a gust-specific distribution cannot buy a consumer anything the method can resolve. Closes #12 (merged into #13 at the 0.7.0 re-cut, review BR-6) as a decision with the number that parks it, per `CLAUDE.md` rule 6. Re-opens only if the wing airload basis itself moves off Schrenk | 2026-08-18 | chat (#13 closure); [`../50_reviews/2026-08-17_backlog_review_0_7_0.md`](../50_reviews/2026-08-17_backlog_review_0_7_0.md) BR-6; `changes/recorded-decisions.*` |

**Still open:** **D-5** (Appendix B twin fixture — blocks long-tail **L-9**);
see the backlog.
