# Critical code review — path to 0.5.0 (2026-08-10)

**Charge:** critical review of the suite against the stated 0.5.0 deliverable —
**the summary report plus sbeam (NASTRAN-style) `FORCE`/`MOMENT` output for the
wing, body and tail load cases** — identifying open decisions and the work
statement required. Conducted per
[`CODE_REVIEW_PROCESS.md`](../10_standard/CODE_REVIEW_PROCESS.md) as four
parallel deep passes (export path, report path, balance/assembly physics,
release/test/CI hygiene) over the full `sloads/export/`, `sloads/report/`,
assembly-path modules, `cli.py`, `app/views/export_report.py` /
`balanced_cases.py`, and their guard tests. Known, already-filed backlog
limitations were excluded from re-reporting; where relevant the review assessed
only whether their in-band flagging is adequate.

**Gate state at review time:** `ruff` clean; **1334 passed, 21 skipped, 0
failed** (2:47, coverage 93 % vs the 80 % gate); `scripts/smoke_test.sh` PASS;
working tree clean at `209604f`. All 21 skips are reasoned fixture-shape skips;
no xfails.

---

## 0. Verdict

The suite is genuinely strong where it says it is: the ULT factor is applied
exactly once on every traced path; the reflection operator, fin axis maps and
unit channels are single-owned with mutation-teeth tests; the equilibrium gate
reads emitted card text; migrations load ten frozen schema fixtures v0→v41; the
report's LaTeX layer is escaped, deterministic and engine-missing-safe.

The holes are concentrated in exactly the places 0.5.0 cares about:

1. **One CRITICAL numeric defect** in a shipped deliverable file (the SI
   mass-check deck's `GRAV`, wrong 25.4×, masked in Imperial, unpinned).
2. **The flagship artifact is a second-class citizen:** the balanced free-free
   deck and the CONM2 mass model are invisible to the report and its manifest,
   unstamped, unreachable from the CLI, and their GID bands sit outside both
   disjointness guards (and collide with the tail-span bands).
3. **The gates are asymmetric:** deck-level closure is re-derived from card
   text for three of six DOF only; the CONM2 family is the one deck family the
   round-trip harness never solves.
4. **The single-owner bug class just fixed (`tail_span._load_factor`) has four
   surviving siblings** in `net_loads`, `wing_inertia`, `body_loads`,
   `balance`.

Per RELEASE_PROCESS §3.2 ("no open [CRITICAL]/[MAJOR] findings"), the findings
below block tagging until fixed, explicitly waived, or descoped by decision.
Separately, `[Unreleased]` already exceeds the §2 cadence threshold (~8 closed
steps, two schema bumps since v0.4.0), so the release-scope decision (§3, D-R1)
should be taken first, not last.

---

## 1. Findings — CRITICAL

### C1. SI mass-check deck `GRAV` acceleration is wrong by 25.4× — `sloads/export/mass_cards.py:403`

> **Closed 2026-08-10** with F-G2 (0.5.0 row 1). `units.DeliverableUnits.gravity`
> is now the single owner of the deck acceleration, drift-guarded per system,
> and the mass deck is solved in CI in both unit systems. This block no longer
> blocks the tag under RELEASE_PROCESS §3.2.
`g = u.force.factor / (u.mass.factor * u.length.factor)` is the
dimensional-consistency *identity* — it equals `G_IN_S2` = 386.0886 in **both**
unit systems (that equality is itself pinned by
`test_the_two_systems_agree_on_gravity_exactly`). It is g in in/s², always —
not g in deck units. The SI deck needs 9806.65 mm/s² and instead writes
`GRAV, …, 386.0886` under a header that says "386.0886 mm/s^2". Correct form:
`u.force.factor / u.mass.factor` (equivalently `G_IN_S2 * u.length.factor`).
Imperial (`length.factor = 1.0`) masks it completely.

**Consequence:** `--export-conm2 --units si` ships a mass-check deck whose
recovered inertia forces are 25.4× low — the artifact whose whole purpose is
the independent CONM2-vs-`inertia_only_cards` comparison "proves" sloads'
inertia wrong by a length factor. This is the D-19 failure class (parses
cleanly, silently wrong) the codebase organizes itself around preventing.
**Pinned:** no — the only GRAV assertion is that the string exists; magnitude
is asserted in neither system, and the mass deck is never solved in CI (F-G2).

---

## 2. Findings — MAJOR

Grouped: **correctness (F-C\*)**, **deliverable reachability (F-D\*)**,
**gate gaps (F-G\*)**, **contract/report (F-R\*)**.

### Correctness

**F-C1. GID band collision: balanced deck vs tail-span decks.** *(Closed
2026-08-10: `sloads/export/bands.py` is now the single band owner, the balanced
deck re-banded to 6001/6201/6401, and `tests/test_bands.py` proves disjointness
over the whole registry — see F-G3, closed with it, plus m5 and m6.)*
`balanced_deck.py:72-81` allocates right wing 4001–4200 / left 4201–4400 /
centreline 4401–5000; `sbeam_bridge.py:1221-1223` allocates htail 4001–4499 /
vtail 4501–4999. Complete overlap, while `tail_span_gid`'s docstring
(sbeam_bridge.py:1229) still claims disjointness "so an assembled airframe can
carry both surfaces at once" — the exact splice both decks' own in-band notes
invite would silently sum two unrelated loads on one node.
`test_gid_blocks_are_disjoint` and `test_mass_eids_are_disjoint_from_every_gid_band`
both hand-enumerate bands and omit the balanced deck, so the guards are blind
to it. **Pinned:** no.

**F-C2. CLI wing export skips the LRA boundary transfer — `cli.py:163-167`.**
`_export_sbeam` passes `build_net_loads(project).wing_net` (a bare list), so
`_as_results`' Project branch — the one that calls `loads_ref_axis_results`
(sbeam_bridge.py:396) — never runs, and the CLI wing deck's torsion, station X
and lever arms are about the 25 %-chord line while the GUI's (via
`report/content.py:332`) are about the LRA. Labelled in-band via the
`torsion_axis` stamp, so not silently wrong, but the module contract ("when the
export is built from a Project, the bridge first transfers … to the LRA") is
defeated on the headless route the sizing loop is supposed to script.
**Pinned:** the Project-path transfer is; which axis the CLI deck is about is
not.

**F-C3. Chordwise tail deck emits the fin's side load as `Fz` —
`sbeam_bridge.py:1177-1183`.** `tail_force_moment_cards` writes every
component through `to_force(0.0, 0.0, fz)`, vtail included —
`coordinates.py:186-190` names this exact pattern as the canonical trap. The
span-deck family fixed it via `tail_force_to_airplane`; the chordwise deck did
not, and nothing in its `$` header states that the vtail's Fz is
surface-normal (lateral in airplane axes). A consumer splicing the fin's
chordwise cards into an airplane-axes model applies a vertical fin load.
**Pinned:** no — `test_tail_deck_resultants` sums v[2] for both components,
enshrining current behavior.

**F-C4. Silent zero-load fallback in the profile-scaling writers —
`sbeam_bridge.py:1100, 1450`.** `scale = (total/total_raw) if
abs(total_raw) > _TOL else 0.0`: a degenerate chordwise profile with a
non-zero target load makes the entire load set vanish with no error, while the
case header still claims a non-zero applied sum — an internally contradictory
deck, violating the raise-loudly contract every neighbouring path honors.
Should raise. **Pinned:** no.

**F-C5. WING-tagged item mass can vanish from the balanced model —
`balance.py:364-376, 709-726`.** `_wing_inertia_scale` returns 0.0 whenever
`panel_both == 0` regardless of WING-tagged items, zeroing every wing-inertia
load, while `assembly_distributes_mass(WING)` simultaneously excludes those
items from `body_inertia` — the whole WING item weight leaves the model,
absorbed silently by the closure, with a note that misattributes the cause.
Violates the B-2 partition guarantee. Edge case (no shipped fixture hits it);
`test_the_inertia_set_weighs_the_case` runs only on shipped fixtures.
**Pinned:** no. **Closed 2026-08-10** — the scale raises when the loading has
WING item mass and the panel is non-positive (user decision: raise, not lump);
the zero-scale note now names the real cause. Building the gate showed the
finding understated it: `panel_weight_lb = 0` did **not** give the zero panel
assumed here, because the density iteration's ±1 % band is empty at a zero
target — it walked the density through zero and returned negative strip masses
(−0.108 lb on `ga6_normal`), which scaled to a ×−3045 sign-flipped wing inertia
set that still weighed the case and so passed `test_the_inertia_set_weighs_the_case`.
`_root_density` now short-circuits a non-positive target. Pinned by
`test_wing_items_with_no_panel_model_raise_rather_than_vanish`,
`test_no_wing_items_and_no_panel_still_weighs_the_case` and
`test_an_empty_panel_weight_gives_an_empty_panel`.

**F-C6. The `project.envelope` single-owner bypass class has four surviving
instances** (same class as the `tail_span._load_factor` n=1.0 defect fixed
2026-08-10; `select.default_envelope` is the declared M2R-8 owner):
- `net_loads.py:65-66` — direct `project.envelope.vn` read; a SELECT-derived
  wing case raises `MissingInputError` on the `run_all_modules` path instead
  of building the envelope (loud, but the derived route is broken exactly
  where tail_span's was).
- `wing_inertia.py:230, 308-309` — same read; `_critical_wing_conditions`
  (:242-246) silently returns `[]` when the envelope isn't persisted, so the
  derived wing-case route quietly yields no cases.
- `body_loads.py:245` — the inverse: always rebuilds via `build_envelope`,
  ignoring a persisted envelope, while `_critical_fuselage` (:229) prefers the
  persisted one — conditions and the nz/lt they're integrated with can come
  from two different envelopes.
- `balance.py:882` — `project.envelope or build_envelope(project)` duplicates
  the owner logic; a persisted envelope with empty `vn` is accepted and every
  condition drops with a misleading "nothing to balance".
**Pinned:** none. **Closed 2026-08-10** — all four routed through the `select`
owners (`default_envelope`, new `default_critical`, `vn_points`/`vn_by_case`),
`tail_span`/`taildist` delegating too, and pinned by
`tests/test_envelope_owner.py` (AST drift guard + a gate per site, six failing
against the pre-fix code). It also fixed `concept_heavy`'s export — D-R6's
diagnosis: the derived route was the broken side — and surfaced a new item
(a wing case row can name a flight condition its loads were not computed at).

**F-C7. Conditions silently drop out of the assembled balanced deck —
`balance.py:902-907`.** A missing V-n point, unknown CG case, or non-derivable
loading each `continue` with no record on the `ModuleResult`, the deck, or the
report; the drop set is pinned for shipped fixtures only (`_EXPECTED_CASES`).
On a user project the primary deliverable can quietly ship missing conditions
(the RJ already loses NMAA this way). Needs an in-band skipped-conditions
record with reasons. **Pinned:** shipped fixtures only.

**Closed 2026-08-10.** `build_balanced_cases(project, skipped)` records a
`SkippedCondition` (component, condition, V-n point, reason code, reader-facing
reason) at every `continue`, and the record is stated on the `ModuleResult` (a
final "Assembly record" condition), in the deck's `$ CONDITIONS NOT ASSEMBLED`
block and in report §4 — emitted whether or not anything was skipped, so "every
condition assembled" is itself a statement. The gate is the property rather than
the fixture: assembled ∪ recorded == SELECT's condition set, disjoint, on every
example (`test_every_condition_is_either_assembled_or_recorded`), so a sixth
drop path cannot be added without a reason travelling with it.

### Deliverable reachability

**F-D1. The 0.5.0 deliverable set is not reachable headless —
`cli.py:232-235`.** `--export-target` offers `wing, tail, htail-span,
vtail-span, control`: **no `body` target** (body FORCE deck, body span CSV and
wing-attach fitting CSV unwritable from the CLI) and **no CLI route at all for
the balanced free-free deck** — the mission's primary deliverable is writable
only from the Streamlit Balanced Cases page. Docstring drift (cli.py:10-21
doesn't mention body either), not a recorded decision.

**F-D2. The balanced deck and CONM2 model are invisible to the report and
unstamped.** `balanced_airframe.bdf` downloads from
`app/views/balanced_cases.py:184` with no G8.3 methods block (per-case ULT
headers only); it appears in no report section, no manifest row, no Export
bundle; the CONM2/mass-check decks likewise. Even with per-component decks as
the 0.5.0 scope, this is a deliverable route emitting load artifacts outside
the controlling document.

> **Closed 2026-08-10** (0.5.0 row 1, decision D-R2). Report **§6 "Balanced
> free-free airframe cases"** states the assembled model per case — `Nz`,
> pre-closure residuals, applied roll couple, closure relief — from
> `balanced_case_rows`, the deck's own row builder, plus the handed twin-pair
> statement and the mass-case identity table (payload case → `MASSSET`, minted
> once in `mass_cards.massset_identity`; non-derivable cases marked NOT
> EXPORTED with their reason). The manifest gained rows for
> `balanced_airframe.bdf` and the three mass files — listed only when the bundle
> will contain them — and the Export bundle now carries all four, stamped and in
> the bundle's resolved system. The two page-level downloads (Balanced Cases,
> Weights → Mass Export) are stamped. No exported byte moved.

**F-D3. L-8g confirmed open and wider than filed.** No headless CSV/BDF
carries the methods/basis stamp (`cli.py:104-175` passes no `header_comment`;
writers already accept it). Additionally unstamped: `--export-conm2` output.
The CLI-only `htail-span`/`vtail-span` targets produce artifacts no GUI
channel, report section or manifest row acknowledges.

**F-D4. `examples/concept_heavy.project.json` still cannot export
(verified).** `--export-sbeam` raises `MissingInputError` from
`net_loads.py:71` (filed backlog defect). One of six shipped fixtures failing
the flagship path is a release-notes item at minimum; it is also excluded from
the round-trip matrix and its wing deck is absent from the Imperial baseline
(filed).

### Gate gaps

**F-G1. Deck-level closure is card-text-verified for only three of six DOF —
`tests/test_balance.py:290-315`.** The gate asserts `fx`, `fz`, `my` only;
`equilibrium.Resultant` already carries all six. The very failure mode this
gate exists for (in-memory closes to 1e-13 while the deck is out 4–22 % from
node collapsing) applied to a fin FORCE card or a reflected port-twin node
would unbalance `fy`/`mx`/`mz` invisibly. One-line fix. **This is the gate gap
itself.**

> **Closed 2026-08-10.** All six DOF asserted (roll/yaw against the semi-span),
> with `test_the_lateral_half_of_the_deck_gate_has_teeth` calibrating the gain
> and the body deck's free-free gate swept the same way.

**F-G2. The CONM2/mass deck family is the one family the round-trip harness
never solves.** *(Closed 2026-08-10 with C1 — see that finding.)* The sbeam `GRAV`-on-`MASSSET` vs `inertia_only_cards`
comparison was done by hand on 2026-08-08 and never automated — which is
precisely why C1 survived. Already the backlog's Pri 2 (plan 12 C6 remainder);
this review confirms it must gate **both unit systems** to catch the C1 class.

**F-G3. Band disjointness is prose + hand-enumerated tests, and it already
failed once (F-C1).** *(Closed 2026-08-10 with F-C1 — the registry plus an
exhaustiveness sweep over every `sloads/export` module's globals, so an
unregistered band now fails CI.)* GID/EID/SID bands are spread across `sbeam_bridge.py`,
`balanced_deck.py`, `mass_cards.py` and re-enumerated by hand in two tests.
Wiring a new deck family currently touches ~9 places. The structural fix
(CLAUDE.md practice 3): a single band registry with one owner and an
exhaustiveness-checked disjointness test.

### Contract / report

**F-R1. `governing_loads_table` ignores the per-case safety factor —
`sloads/report/render.py:196-241`.** *(Closed 2026-08-11 — every row now scales
by its own `CriticalCondition.safety_factor` and states it; the caller-supplied
`sf` override is removed, and a test pins a `SF = 1.0` case.)* Applies flat `sf=ULTIMATE_FACTOR` to
every `CriticalCondition` against the model contract (results.py:212-216). No
number is wrong today (SELECT never sets a non-default SF), but the first
non-1.5 critical case (M4-8 Layer 2, already-ultimate cases) is silently
mis-scaled and mis-labelled in report §5 and both GUI views —
tests/test_results_review.py:69-70 pins the hole (asserts every row SF ==
1.5), not the contract. Direct collision with M4-8's acceptance ("report/ and
sbeam_bridge produce identical factors for the same case").

**F-R2. Bundle-manifest cross-references off by one section —
`report/content.py:1393-1448`.** After the §2 sign-conventions insertion, the
manifest still points companion files at §3/§4/§5 for what are now §4/§5/§6.
No test pins the § values.

**F-R3. The methods statement omits the standing "not a certification
document" disclaimer** (SUMMARY_REPORT.md §4.6 item 9) — it exists on the
title page only (`latex.py:113-117`), not in `methods_statement`
(methods.py:199-311), which is what travels in METHODS.txt, every CSV `#`
block, every BDF `$` block and the workbook. The stamp guard test enumerates
only the implemented blocks, pinning the omission.

**F-R4. `_STANDING_LIMITATIONS` (methods.py:73-82) is not "every open
caveat".** Missing, all confirmed open: fin-only lateral aero (L-7),
assumed-rectangular tail planforms (the tail_span "ASSUMED" marker never
reaches the report), the wing-stick centreline clamp (the manifest describes
`wing_stick.bdf` as "geometry only" with no clamp caveat anywhere in the
report), and the lumped aileron wing-AC couple. No completeness test.

**F-R5. SELECT h-tail critical conditions have no balanced/handed
representation, and the gap is stated nowhere.** `build_balanced_cases`
handles `wing` and `VTAIL` components only (balance.py:893-901). The h-tail is
covered implicitly through `vn.lt` on wing conditions, but 23.427(a)
UNSYMMETRICAL — a genuinely handed case the full-span tail topology was built
to carry — has no assembled representation. ONENGOUT's and ground cases'
exclusions are documented; this one is not filed.

**F-C8 (cadence).** `[Unreleased]` holds ~8 closed steps and two schema bumps
(v43, v44) two days after v0.4.0 — past the §2 "~5 closed steps" threshold.
Unreleased work has no regression baseline.

---

## 3. Findings — MINOR (consolidated)

Export path:
- **m1.** Balanced-deck SUBCASE ids are positional (`BALANCED_SID_BASE + i`,
  balanced_deck.py:84, 276-287) — reintroduces the renumbering instability
  M4-2 removed everywhere else; the `case_ref`/`_handed_ref` machinery to mint
  stable ids exists. The closure test builds its map positionally too.
- **m2.** CLI error contract inconsistent (cli.py:143-167): `control` swallows
  any `ValueError` (bad input == absent input), all-skipped yields an uncaught
  traceback; wing/tail raw-traceback `MissingInputError` vs `_export_conm2`'s
  caught-and-printed. One contract.
- **m3.** `stick_model_bdf` takes GRIDs from `results[0]` only
  (sbeam_bridge.py:696-697): differing per-case station lists yield FORCE
  cards on nonexistent or wrongly-placed GRIDs; the writer should assert
  station-set agreement.
- **m4.** CONM2 cards are single ~140-char free-field lines
  (mass_cards.py:255-258) — fine for sbeam's reader, non-portable to MSC/NX;
  the 72-col sweep checks `$` comments only, never card lines.
- **m5.** ~~`station_gid` (wing band) alone lacks a capacity guard
  (sbeam_bridge.py:256-258) — would walk into the body band at ~999 stations.~~
  **Closed 2026-08-10** with the band registry: every allocator goes through
  `Band.allocate`, which raises.
- **m6.** ~~Trapezoid-tributary-plus-rescale logic duplicated verbatim between
  `_tail_nodal_forces` (1087-1101) and `_control_nodal_forces` (1437-1451)~~
  **Closed 2026-08-10**: one `_trapezoid_tributary_forces` owns it. The wider
  "five near-identical writer families" consolidation was **not** done — the
  band registry took the id half only; the writer-family merge stays open and
  unscheduled.

Balance/assembly:
- **m7.** Tail load applied at `fl.xtc` unconditionally (balance.py:729) while
  `_balance` used `xtf` when flaps down — latent until a flap-config balanced
  family exists; nothing checks `point.config`.
- **m8.** Two independently-entered sources for the tail-load arm:
  `tail_loads.xt25` (body_loads.py:93-96) vs `flight_loads.xtc` (trim,
  balance) — duplicate-entry class, no reconciliation guard.
- **m9.** Zero-ballast loading derivation checks `|xa − xcg| ≤ 0.5 in` and
  never checks z (mass_distribution.py:648-651) while `case_loading_checks`
  asserts zcg at 1e-9 — derivation gate and closure-decoupling assumption
  inconsistent by construction (a violation fails loudly downstream).
- **m10.** Tail strip inertia carries no chordwise torsion term
  (tail_span.py:229-237) where WINGINER models the analogous `−w·(c50−c25)` —
  small, unmodelled, unstated in the notes and not among the filed empennage
  limitations.
- **m11.** Derived-route ACRL assembles as a silently symmetric unhanded case
  (UNB=0) while `unbalanced_rolling_moment`'s docstring (balance.py:670-674)
  claims that cannot happen — skip zero-UNB rolling conditions or fix the
  claim and note the case.
- **m12.** `derive_case_loadings` is uncapped 2^n over discretionary items
  (mass_distribution.py:642) — cap with a loud refusal.
- **m13.** Symmetric-family pitch ceilings are shared per fixture+family, so
  an individual case regressing within the shared ceiling passes; per-case
  pins (as `_LATERAL_CASE_NUMBERS` already does) would bite harder.

Report/bundle:
- **m14.** Workbook Project-sheet unit statement is HUMAN-channel while the
  span-load sheets are solver-channel (export_report.py:353-357) — a
  §4.7-class conformance failure in SI.
- **m15.** NaN in any value silently converts the whole report to "could not
  be built" (render.py:44 via `_try`); Inf raises OverflowError, in neither
  `_CALC_ERRORS` set → page traceback. Two divergent `_CALC_ERRORS`
  definitions (content.py:60 with TypeError, export_report.py:84 without).
- **m16.** Fourth partially-duplicated SI factor map (content.py:206-211) atop
  units.py's three (already filed); units-split prose triplicated
  (methods.py:128-134, content.py:1460-1465, export_report.py:301-309).
- **m17.** PDF staleness key includes the minute timestamp
  (export_report.py:200, 336) — a later-minute rerender silently drops the
  compiled PDF from the zip.
- **m18.** `escape` silently drops unmapped non-ASCII (plots_tex.py:70) —
  "Aéro" → "Aro" on a controlled document's title page.

Hygiene:
- **m19.** `.DS_Store` is tracked and not gitignored; `CODE_REVIEW_2026-07-21.md`
  and `PROJECT_REVIEW_2026-07-19.md` sit at root while `docs/50_reviews/` is
  the home; `requirements.txt` is a stale duplicate dependency source
  (streamlit ≥1.30 vs pyproject ≥1.36, lists pytest, omits plotly/openpyxl).
- **m20.** RELEASE_PROCESS §1 stale pointer (`sloads/models.py` →
  `sloads/models/project.py:205`); pyproject classifiers list 3.9 only vs the
  3.9/3.11/3.12 CI matrix; `scripts/smoke_test.sh` is a checklist gate no CI
  workflow runs.
- **m21.** `app/` is outside the ruff gate (`ruff check sloads/ cli.py`) —
  confirmed drift: unused import `build_tail_span` at export_report.py:43.

NITs (recorded, not scheduled): `_force_moment_lines` docstring "unscaled"
imprecision; `_root_node` half-strip-inboard crossing the centreline;
`equilibrium.resultant` not validating MOMENT GIDs (deliberate — comment it);
`wing_sets` unused `condition` param; `handed_twin` −0.0 normalization
inconsistency; balance docstring mis-citing `coordinates.reflect_load`;
misleading zero-scale note text; stale § refs in methods.py/coverage.py
docstrings; `plots_tex._num` docstring's "never exponent form" claim;
content.py:832 private `_gust_ude` cross-module import; control rows' blank
`case_ref` vs `LC{n}` fallback inconsistency.

---

## 4. Open decisions — all resolved 2026-08-10

> **Resolution record.** All eight were put to the user and resolved in the
> same session; the register of record is
> [`03_resolved_decisions.md`](../25_notes/03_resolved_decisions.md)
> (**D-R1…D-R8**), and the backlog priority table was re-cut to match (rows
> 1–19 = the 0.5.0 release scope). Outcomes: **D-R1** hold 0.5.0 for the
> deliverable (option b); **D-R2** balanced deck + CONM2 first-class (a);
> **D-R3** ground cases out (a); **D-R4** map the fin's chordwise cards to Fy
> (a); **D-R5** CLI transfers to the LRA (a); **D-R6** diagnose-then-fix for
> `concept_heavy` (a); **D-R7** mint SUBCASE ids from the `CaseRef` (a);
> **D-R8** **build the 23.427(a) h-tail case in 0.5.0** (option b — the user's
> call against this review's file-and-state recommendation; adds the release's
> one L-tier design-note-gated step). The option texts below are kept
> unedited as the statement of record.

- **D-R1 — Release scope.** The cadence rule says cut now; the stated 0.5.0
  deliverable is not yet coherent (C1, F-D1/D2, F-G1/G2). Options:
  (a) **two-release path:** cut 0.4.1/0.5.0 now from `[Unreleased]` as-is
  (with C1 fixed first — it is shipped-wrong today) and aim the deliverable
  completion at 0.6.0; or (b) **hold 0.5.0** for Phases 0–2 of the work
  statement (§5), accepting ~2–3 sessions more unreleased growth.
- **D-R2 — Balanced deck + CONM2 in the 0.5.0 deliverable set?** The mission
  statement says the full-span balanced model is the primary deliverable; if
  so, F-D1/F-D2 (CLI targets, report section, manifest rows, stamps) are
  0.5.0 scope, not polish. If 0.5.0 is per-component decks only, both
  artifacts still need at minimum stamps + a manifest acknowledgment.
- **D-R3 — Landing/ground cases in or out of 0.5.0.** "Body loads" without
  the ground case family (backlog Pri 4/5, effort L) is a flight-only body
  deck. Recommendation: out — ship flight-only with the exclusion stated in
  the limitations block and release notes; Pri 4/5 define 0.6.0.
- **D-R4 — Chordwise fin deck axis (F-C3).** Map the vtail's load to Fy via
  `tail_force_to_airplane` (byte-changing, consistent with the span family)
  vs document the per-component local frame in the deck `$` header
  (byte-preserving). Recommendation: map to Fy; the trap is named in
  `coordinates.py` itself.
- **D-R5 — CLI wing-deck axis (F-C2).** Make the CLI transfer to the LRA
  (changes shipped CLI bytes; aligns both front-ends and the contract) vs
  document the divergence. Recommendation: transfer — the sizing loop is the
  mission.
- **D-R6 — `concept_heavy` (F-D4):** fix the fixture (add `case` references
  into `envelope.vn`) vs state the exclusion in the release notes. Decide
  whether the derived-case route gap is the fixture's or the route's.
- **D-R7 — Balanced-deck SUBCASE identity (m1):** move to minted
  `subcase_id(case_ref)` numbering now (byte-changing, one digest
  regeneration) or accept positional ids for 0.5.0.
- **D-R8 — h-tail handed family (F-R5):** file-and-state for 0.5.0 (a
  limitations line + backlog item) vs build the 23.427(a) assembled case
  (new work, pairs with Step 9).
- **Standing decisions already in the backlog that 0.5.0 inherits** (no new
  input needed here, listed for completeness): Pri 12 loading-definition vs
  fixture fix; ACRL derived-route point identity; ATR-42 Mach-capped
  infeasible points; gust spanwise distribution; flutter-Mach basis; M4-3 VSF;
  D-5 Appendix B.

---

## 5. Work statement to 0.5.0

Phased; each item names its closure tier. Phases 0–2 are the release
content; Phase 3 is mechanics. Estimated total: **~5–7 working sessions**
(the D-R8 build adds ~1–1.5 to the original 4–6; ground cases stay out per
D-R3). The authoritative ordered form of this work statement is now the
backlog priority table, rows 1–19.

### Phase 0 — correctness and gates (do first, order within phase free)
| Item | Findings | Tier / effort |
|---|---|---|
| ~~Fix SI `GRAV` factor + assert magnitude in both systems~~ **closed 2026-08-10** | C1 | S (fix) — the test lands with Phase 0's G2 item |
| ~~CONM2 round-trip CI leg, both unit systems (backlog Pri 2)~~ **closed 2026-08-10** | F-G2, C1 guard | M / S |
| ~~Six-DOF card-text closure gate~~ **closed 2026-08-10** | F-G1 | S (one-line + expected values) |
| ~~GID/EID/SID band registry + exhaustive disjointness test; re-band the balanced deck out of 4001–5000~~ **closed 2026-08-10** | F-C1, F-G3, m5, m6 | M / M |
| ~~Envelope single-owner sweep: route all four reads through `select.default_envelope` + guard test per the tail_span fix pattern~~ **closed 2026-08-10** (owners `default_critical`/`vn_points`/`vn_by_case` added; `tests/test_envelope_owner.py` AST drift guard + per-site gates; also fixed `concept_heavy`'s export, D-R6) | F-C6 | M / S–M |
| ~~Raise on degenerate profile in `_tail_nodal_forces`/`_control_nodal_forces`~~ **closed 2026-08-10** (in their shared owner `_trapezoid_tributary_forces`) | F-C4 | S |
| ~~Guard WING-item mass partition when `panel_both == 0` (raise or lump, never vanish)~~ **closed 2026-08-10** — raise, and `_root_density` no longer returns a negative panel for a zero target | F-C5 | S–M |
| Skipped-conditions record on `build_balanced_cases` (ModuleResult + deck `$` + report) | F-C7 | M / S |

### Phase 1 — deliverable completion (scope per D-R2/D-R3)
| Item | Findings | Tier / effort |
|---|---|---|
| CLI `body` target; CLI balanced-deck + mass-deck targets | F-D1 | M / S–M |
| CLI/headless methods stamp (L-8g, backlog Pri 25) incl. `--export-conm2` | F-D3 | S–M |
| CLI wing export through the LRA transfer (per D-R5) + axis-pinning test | F-C2 | M / S |
| Balanced deck + CONM2 into the Export bundle, manifest, and a report section (balanced-case table: per-case n, residuals, hand pairs, mass-case identity) | F-D2 | M–L / M |
| Fin chordwise-deck axis per D-R4 | F-C3 | M / S |
| `concept_heavy` per D-R6 (+ its Imperial-baseline artifact, backlog Pri 17) | F-D4 | S |
| h-tail 23.427(a) handed balanced family — **added by D-R8**; design note before code | F-R5 | L / M |

### Phase 2 — the report tells the truth about itself
| Item | Findings | Tier / effort |
|---|---|---|
| Manifest § renumber + pin the § values | F-R2 | S |
| Disclaimer block into `methods_statement` (stamp guard updated) | F-R3 | S |
| Limitations completeness: add L-7 lateral, assumed planforms (surface the in-band ASSUMED marker), centreline clamp / `wing_stick` caveat, lumped aileron couple, flight-only body deck (per D-R3) — the h-tail gap needs no limitation line, it is built instead (D-R8) | F-R4 | M / S–M |
| Per-case SF in `governing_loads_table` (pre-req slice of M4-8 Layer 1; full resolver stays backlog Pri 9) | F-R1 | M / S |
| Workbook solver-channel unit statement | m14 | S |

### Phase 3 — release mechanics
Root cleanup (`.DS_Store` untrack+ignore, move the two root review files to
`docs/50_reviews/`, delete or regenerate `requirements.txt`) — S;
RELEASE_PROCESS §1 pointer + classifiers — S; then the §4 steps: version bump,
changelog date (release notes must state: flight-only body deck, concept_heavy
status per D-R6, the lateral-aero and h-tail-family caveats), tag, archive
verification. Smoke test into CI (m20) is a fast-follow, not a gate.

### Explicitly deferred beyond 0.5.0 (stay in the backlog, unchanged)
Ground/landing families (Pri 4/5), Step 9 T6–T7, Step 12 LRA bridge, Step 13
SOB node, Step 14 real stiffness, M4-8 full resolver, and every [V] item not
named above. MINOR findings m7–m13, m15–m18 and the NITs are filed by this
document; sweep opportunistically or promote individually.

---

*Review conducted 2026-08-10 by Claude (four parallel deep passes; no files
modified by the review itself). Gate snapshot: 1334 passed / 21 skipped,
ruff clean, smoke PASS, tree clean at `209604f`.*
